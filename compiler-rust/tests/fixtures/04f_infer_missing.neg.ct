// 04f_infer_missing.neg.ct —— v0.7 修订三:TPar 不出现于实参位 → E2060 要求显式
//@ fail: E2060
//@ msg: 无法推断

fn make_box[V](x: I32) -> Box[V] {
    return Box[V](x)
}

test "missing candidate requires explicit typeargs" {
    let b = make_box(1)
    let _ = b
}
