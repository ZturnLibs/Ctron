// main.ct —— 入口 shim(仅此而已)
//
// 形态裁决(2026-09-26 探针实证):发射器「主文件直发」路径对较大依赖图确定性
// SIGKILL 截断(40960B 等,内容相关;c-rust-divergences @20480B 家族再现,
// minimal repro = 本包历史版本),而「依赖路径」全量发射安好——故全部逻辑
// 下沉 todo_app.app(依赖),主文件只留入口转发。详见 docs/c-rust-divergences.md。

use todo_app.app.{ app_main }

fn main() {
    app_main()
}
