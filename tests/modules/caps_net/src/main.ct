// caps_net - E4010: use std.net.Net with ref param, manifest caps only has fs
//@ fail: E4010
use std.net.Net

fn touch(net: &Net) -> I32 {
    return 0
}

fn main() {
    let n = Net_probe()
    let r = touch(n)
    assert(r == 0)
}
