// r6i_interp_nested_str.neg.ct —— 插值片段内嵌套字符串字面量(§1.4 白名单:仅
// 标识符/字段/属性/方法链/索引;§4.11:片段不支持嵌套 `{}`)
// 语义承诺:`"outer { "inner" } tail"` → E1001(串在第二引号终止,片段非法续读;
// C 宿主/R 线均报「未终止的插值」)
// 锚点:自举线把内层片段读成名称,误报 E2020(unresolved: inner)——拒绝但码不符
// → 红;自举线对齐 E1001 转绿。
//@ fail: E1001
//@ msg: 未终止的插值

fn main() -> Void {
    let a = "outer { "inner" } tail"
    println(a)
}
