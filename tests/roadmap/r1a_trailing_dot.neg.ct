// r1a_trailing_dot.neg.ct —— 行尾 `.` 非法(§1.6 换行规则:首点式合法,行尾点非法)
// 语义承诺:`let s = "a".` 行尾悬挂点 → 解析错误
// 锚点:宿主拒绝(解析错误);自举解析器吞点续行误放行 → 红;自举补守卫转绿
//@ fail: E1001

fn main() -> I32 {
    let s = "a".
    println(s)
    return 0
}
