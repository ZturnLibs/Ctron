// W2:--dump-gui 折叠 IR 黄金(view + style;decls = view1 + style2 + main1 = 4)
view Counter {
  <vbox class="root">
    <button class="btn" on:click={inc}>+1</button>
  </vbox>
}
style root { direction: column gap: 8 }
style btn { bg: "#2563eb" }

fn main() -> I32 {
    return 0
}
