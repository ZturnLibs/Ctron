// gui expect: E8193 —— SL-6c 独立 .ctml 警告级:each 体含 input(双形态差分)
view P {
  <vbox class="root">
    <each item in={items}>
      <input bind={draft} class="field"/>
    </each>
  </vbox>
}
style root { direction: column }
style field { bg: "#2a2a2e" }
