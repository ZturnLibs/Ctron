view Counter {
  <vbox class="root">
    <label>count: {count}</label>
    <button class="btn" on:click={inc}>增加</button>
  </vbox>
}
style root { direction: column; gap: 8; padding: 16 }
style btn { bg: "#2563eb" }
