# 服务器泳道 P5 实施计划(std/db 数据访问层)

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:subagent-driven-development(既定模式)。Steps 用 checkbox 跟踪。

**Goal:** 落地路线 P5——std/json 数值保真(前置)、std/uuid(v4 真熵源/v7)、std/crypto 二进制面(HMAC-SHA256/PBKDF2)、PostgreSQL 线协议 v3(简单+扩展查询/SCRAM/事务/取消)、Redis RESP2、连接池(有界+背压+回池复位)、行→struct 绑定(运行时;设计偏差见 D-P5-1)、协议夹具回放(CI 零真库)。出口 = 夹具回放全绿 + nightly 真库互操作 + 简单查询回环 vs libpq ≤1.5× + 行映射负例锚 + 双矩阵回归。

**Architecture:** 驱动**纯 Ctron**(std/db/),传输经 std/net 句柄;测试可注入——回放形态 = 字节流脚本源与真 fd 源共驱(核心帧解析共享,双薄源)。SCRAM 密码学 = std/crypto 二进制面(本波补齐)。**D-P5-1 设计偏差(登记)**:设计稿承诺"comptime 行映射,列缺失/类型不符编译期报"——但 §8.4 语言决策明文禁止泛型反射(类型产出函数预留 v2),该承诺在现行语言下不可达;落**运行时绑定**(列缺失/类型不符运行时 Result 报),`@derive(DbRow)` 插件(编译器已知 derive 集扩展)列编译泳道志向;设计文档回写随本波。

**Tech Stack:** Ctron、PostgreSQL wire protocol v3(公开规范)、RESP2、RFC 5802(SCRAM)、RFC 6234/4231/6070(密码学向量)、nightly 真 Postgres/Redis。

## Global Constraints

- pathspec/hunk 纪律;CI 全夹具回放零真库(nightly 真靶另标);密码学实现一律官方向量锚定(RFC 6234 SHA / 4231 HMAC / 6070 PBKDF2)。
- 发射面缺口清单(divergences (f)(g)(h)(i))继续为编码约束;**新约束 (i) 族**:解释器按值域定宽乘法在 2^28–2^31 带炸——协议长度/计数算术一律 C10 界内惯用法(符号分支/乘前界门,P4 收口波同款)。
- std/db 能力:`db.connect` 键已在 S0 落键集;门面函数首参 `db: StdDb` 贯穿(镜像 net/tls 惯例;加载器严格互斥树——std/db 不 use std.net,句柄按值传入)。
- 池纪律:有界容量必填、等待经 Channel、回池前复位(事务回滚/语句清理)、禁止跨取消复用脏连接。

---

### Task 1 (P5-A): std/json 数值/布尔保真(前置工项)

**Files:**
- Modify: `std/json.ct`(数值/布尔类型化访问器:jget_i64(路径,溢出检测)/jget_f64/jget_bool——现 parse 保留 n/b 标签但值恒 Str,访问器负责 Str→I64 定宽检测(越界=Err 非 silent)/Str→F64;jget 路径语法沿用 qtag/qval)
- Create: `tests/json_fidelity/`(或并入既有 json 测试面):溢出负例(2^63/2^64 边界)、精度负例(F64 舍入可检)、布尔/null 判别、嵌套路径
- Modify: `docs/c-rust-divergences.md`(若实现踩新发射面坑随记)

**Steps:**
- [ ] 访问器 + 负例锚(含 9223372036854775808/99999999999999999999 两串)
- [ ] tests/http 同款双臂跑法(纯层)或并入对应测试面;提交

### Task 2 (P5-B): std/crypto 二进制面 + std/uuid

**Files:**
- Modify: `std/crypto.ct`(二进制面:sha256_bytes/hmac_sha256(key,data → bytes)/pbkdf2_sha256(salt,iter,dklen)——内部复用既有 sha256;hmac 按 RFC 2104 ipad/opad;**注意 (i) 族**:迭代计数与字节索引算术 C10 惯用法)
- Create: `std/uuid.ct`(v4:真熵源——新内建或 ctron_net.c 邻的 ctron_entropy.c 垫片(getrandom//dev/urandom 一次取 16B);v7:unix_ms 毫秒 + 随机尾;格式化/解析)
- Create: `tests/crypto_vec/`(RFC 6234 SHA 向量、RFC 4231 HMAC 向量、RFC 6070 PBKDF2 向量、"abc" sha256、uuid 唯一性 10 万枚 + 版本位断言)
- [ ] 实现 + 向量全绿 + 提交

### Task 3 (P5-C): 协议夹具回放框架 + Postgres wire v3 核心

**Files:**
- Create: `std/db/pg.ct`(线协议:startup(参数面)/AuthenticationOk/简单查询(Query)/RowDescription/DataRow/CommandComplete/ReadyForQuery/ErrorResponse 解析(字段码表)/Terminate;帧读写经注入源:`pg_feed_fd`(真 net)/`pg_feed_script`(回放:十六进制帧脚本 List[Str] 逐帧出)——核心解析共享,双薄源)
- Create: `std/db/` 能力门面雏形(`StdDb` + db.connect 锚;DSN 解析 postgres://user:pass@host:port/db)
- Create: `tests/db/replay_fixtures/**`(夹具:录制格式 = 帧脚本文件;用例:startup 握手/简单查询全行集/错误响应面/协议错帧拒收)+ `tests/db/run.sh`(回放跑法,零网络)

**Steps:**
- [ ] 帧解析核心 + 注入源双形态(真源仅冒烟级;全面测试走回放)
- [ ] 夹具录制:格式定义(VENDORED 式 README)——录制来源 = nightly 手工真库导出或手工按协议文档构造(二选一,登记)
- [ ] 回放用例全绿(解析器对录制字节逐字节正确)
- [ ] 提交

### Task 4 (P5-D): SCRAM-SHA-256 + 扩展查询 + 事务

**Files:**
- Modify: `std/db/pg.ct`(SCRAM-SHA-256 全流程:client-first/reckon salted-password PBKDF2/client-final 校验 server-final——RFC 5802/7677,channel-binding 'n' 档)、扩展查询(Parse/Bind/Describe/Execute/Sync + 预编译句柄)、事务面(begin/commit/rollback;取消传播:scope 取消 → 查询中止 → 连接标记脏 → 回池前复位)
- Create: `tests/db/replay_scram/**`(RFC 5802 测试向量改制协议帧 + 错误口令路径)

**Steps:**
- [ ] SCRAM(向量:RFC 7677 示例 + 错误口令负例)
- [ ] 扩展查询 + 事务 + 取消传播(回放夹具)
- [ ] 提交

### Task 5 (P5-E): Redis RESP2 + 连接池 + 行映射(运行时)

**Files:**
- Create: `std/db/redis.ct`(RESP2:GET/SET/DEL/EXPIRE/INCR;简单/批量/整数/错误/nil 五形)
- Create: `std/db/pool.ct`(有界池:容量必填、Channel 等待、回池复位三件套、脏连接不复用)
- Create: `std/db/rowmap.ct`(运行时行→struct:列名匹配/类型分派/NULL→Option 语义;列缺失/类型不符 → Result 报;**D-P5-1 偏差登记**:运行时而非编译期,@derive(DbRow) 志向)
- Create: `tests/db/redis_replay/**`、`tests/db/rowmap/**`(负例锚 r7d_db_rowmap:列缺失/类型不符 → Result Err 面断言——锚语义从"编译期报"改"运行时 Err",同步改锚注释与 COVERAGE 口径)

**Steps:**
- [ ] RESP2 + 回放用例
- [ ] 池(等待/复位/脏标记回放用例)
- [ ] 行映射 + r7d 负例锚语义修正
- [ ] 提交

### Task 6 (P5-F): 门禁 + 登记收口 + P5 终审

- [ ] nightly 真靶脚本(tests/db/nightly/:真 Postgres/Redis 连接冒烟;本地有则跑,无则 SKIP 登记口径)
- [ ] 性能基准:简单查询回环 vs libpq 同构 ≤1.5×(本地有 libpq 则测;无则登记跳过口径)
- [ ] COVERAGE P5 行、divergences 收口、设计文档回写(**D-P5-1 偏差 + 11-net/12-db 对应注**)、计划执行记录 P5 出口判定
- [ ] pathspec 提交;P5 终审(全波包)

## Self-Review

- 设计覆盖:§六 P5 全项(json 保真/uuid/crypto 面/PG v3/SCRAM/扩展查询/事务/RESP2/池/行映射/r7d 锚)有任务;规范 12-db.md 定稿随 Task 3-5 落地节奏(草案转定稿在 Task 6)。
- D-P5-1 是本波最大设计偏差——计划已登记,设计文档回写是 Task 6 必做项。
- 风险前置:SCRAM/PBKDF2 全向量锚;回放框架先于驱动核心(Task 3 顺序保证 CI 零真库);(i) 族乘法规约写进 Global Constraints。
- 明确不做:MySQL/Mongo、订阅(pub/sub 列 P8)、@derive(DbRow) 插件实现(志向)、libpq 绑定形态。
