# Ctron C 版:C4 执行层实施计划(C4-a ✅ / C4-b ✅ / C4-c ✅ / C4-d ✅ / C4-e ✅ / C4-f ✅ / C4-g ✅ / C4-h ✅)

> **决策上下文**:见 `compiler_c/README.md`。执行层(行为/panic 语料运行)按语言域切分推进;C4-a 先覆盖**纯数值/逻辑/字符串/范围**域,GC/并发/own/trait 对象等域留待后续增量,允许表显式列明,杜绝“假绿”。

**Goal:** `src/rt.c` 解释器 + `suite_rt` 运行器:对允许表内 .ct 逐 test 块执行,panic 文件要求 panic 消息含 `//@ panic:` 子串;未支持文件计 deferred(不运行、不计失败)。

**允许表(全部真实通过)**:C4-a `01_basics`/`01_overflow.panic`/`02d_divzero.panic`/`03b_numeric_widths`/`04b_logic`;
C4-b 新增 `01d_strings`/`01e_multiline_chain`/`02b_option_propagation`(合计 24 个 test 块)。

## C4-c:struct/class/Box/字符串 API/match 结构模式
允许表新增 `02e_match_patterns`/`03_values_refs`/`03c_str_string`(合计 32 test 块全过):
- `V_STRUCT`(struct 值 vs class 引用,含 `is_class`):字面量构造、字段读写、成员赋值
  (`b.x = 10`);赋值/绑定语义 = struct **值拷贝**(clone_val 深拷贝)、class/Box **引用共享**;
- `V_BOX`(Box[T](v) + 自动解引用:字段读/结构模式穿透);
- 枚举单位变体(Red/Green/Blue)、整型字面量模式(0/7/`_`)、`Pt { x, y }` 结构模式(简写绑定);
- `Str` `.char_len`(UTF-8 标量)/`.len`(字节)/`.to_string()`(String 持有,隐式降格 Str)。

## C4-h:函数值调用 / 索引写透 / 字符串切片 utf8 / doc-test 样例
允许表新增 `00_doctest`/`03f_slices`/`03g_fn_types`/`03h_utf8_boundary.panic`(合计 62 test 块全过):
- 绑定函数值调用:fn(I32)->I32 类型参数收具名函数/闭包,EX_CALL 经绑定值分派
  (invoke_vals 支持多参闭包);apply_twice/combine 通过;
- 索引赋值与复合(`arr[i] = v`/`seen[b] += 1`)写透共享后备(切片视图与数组同后备,
  03f 的 view[1]=20 对 buf 可见);
- `Str.slice(lo..hi)` 字节切片 + **utf8 字符边界校验**(界内续字节 → panic "invalid
  utf8 boundary");Arena.zeros/array[T](n) 与 Arena.fixed(n) 显式 arena;
- 00_doctest 普通行为锚样例通过。

**待规范裁定(不硬凑)**:`08_bare.ct` 自校验注释("频次之和=元素个数"=4)与实际代码
`for b in data { acc += seen[b] }`(重复元素被重复求和 → 6)相悖;C 实现忠实执行语义得 6,
拒绝伪造 4。待语料/规范所有者裁定后并入允许表。

## C4-g:泛型运行形态 / static·Global / derive(Show)
允许表新增 `03e_generics_types`/`06d_globals`/`07_capabilities`(合计 57 test 块全过):
- 泛型 struct/class 字面量按运行类型直接构造(bound/derive 仅为编译期);泛型 fn 调用
  `render(p.second)` 运行时同普通函数;`@derive(Show)` → `v.show()` 内建格式化(非空);
- `static let` 只读全局(EX_IDENT 兜底求值);`Global[T](name, init)` = 与 Mutex 同存储容器;
- `with_mut` **写回语义**:参数副本运行后回写存储(整值自增/类共享均持久)。

## C4-f:并发运行时(scope/spawn/join/Channel/Mutex/取消)
允许表新增 `05b_panic_join`/`06_concurrency`/`06e_cancel`(合计 51 test 块全过):
- 协作式任务:scope 内 spawn 注册任务(闭包捕获环境执行);join 运行并重抛 panic、
  join_or 捕获为 Err(TaskPanic)并**取消作用域**(兄弟失败 → 传播);
- Channel[T](cap) → (Sender,Receiver)(V_TUPLE + 元组模式绑定);阻塞 send/recv
  在满/空时**让出调度**运行就绪任务,作用域已取消则返回 Err(ScopeCancelled)——06e
  的"已填满通道的 blocker 不悬挂"成立;R 内嵌套 setjmp 实现任务级 panic 捕获;
- Mutex[T](v):with/with_mut 闭包共享内部类实例(类成员写放开);
- `for _ in` 通配模式;`panic(msg)` 内建。

## C4-e:own/arena 块、List 容器、Atomic 与 RAII drop
允许表新增 `05_own`/`05g_into_gc_isolation`/`06g_noalloc_trait`/`05i_deep_cause`/`05d_drop`(合计 45 test 块全过):
- `own (arena) { … }` 求值 = 块执行(arena 操作经既有表达式/语句通道);
- `V_LIST` 可变容器:arena.list/GC list 同表示;`.push`(arena 内增长)、`.len`/索引、`.into_gc()`
  = **深拷贝**(05g 独立语义:GC 侧与 arena 原件互不影响);
- `V_ATOM`:Atomic[T](init)/load/store/fetch_add(共享单元);
- **RAII**:impl Drop 的类型,块作用域退出按声明**逆序**自动调用 `drop`(05d:借助共享 Atomic
  观察 b 先 a 后);
- 修 context 载荷悬挂 bug(cause 载荷由栈拷贝入 arena)。

## C4-d:Result/Error 链 + trait/impl 方法·prop 运行时
允许表新增 `02_option_result`/`03d_props_traits`(合计 37 test 块全过):
- Result 语义:Ok/Err 标签(或默认 or/expect/map 扩展至 Result);`?` Ok 解包 / Err 提前返回;
  `.context(msg)` 把 Err 载荷包装为 `V_ERR` 错误链(message + cause: Option);错误 prop 读取
  `e.message`/`e.cause`、`is_some/is_ok`。
- 类实例方法分派:按实例类名解析 impl 项方法 → trait **默认方法体**(空 impl 合法);
  绑定 `self` 后执行;方法体可经 supertrait 读 prop/调方法;prop 实现体(`env.name` 无括号)
  与字段读走同一成员通道;trait 对象接收者(值即实例)同样适用。

## C4-b:GC 复合值 / Option / match / 闭包 / 函数值

- 数组/切片值:`V_ARR`(arena 元素块),定长数组字面量、索引、`.len` 属性、`for x in xs` 迭代、作 `I32[]` 参数透传;`Str.len` 字节长。
- 标签值 `V_TAG`(变体名 + 载荷表):`Some/None/Ok/Err` 与文件枚举变体(`Advance(n)`/`Stop`)构造与裸值;Option 方法内建 `map/or/expect`;`?` Try(空 → 提前 return);match + 模式绑定(`Some(v)`/`Advance(n)`/unit/通配/字面量)。
- 函数值:命名函数 `V_FN`(`map(twice)`)与闭包 `V_CLOSURE`(捕获环境指针,追加式环境不回收,指针即快照)。
- 插值 `{expr}` 经包装再解析于当前环境求值(支持成员链/索引/方法调用)。


## 已实现语义(值/执行域)

- **值**:I8..U64/ISize/USize(64 位宽)、F32/F64、Bool、Str(arena)、Void、range 值。
- **数值**:默认 I32 / 字面量后缀;绑定与参数按声明类型**自适应**(域外 panic);检查算术溢出 panic("overflow");`+%`/`-%` 回绕;除法/取模除零 panic("division by zero");`%` 符号随被除数;浮点字面量与运算。
- **表达式**:优先级、比较、`&&` 短路、一元、`if/else` 值、块尾值、range 值、插值字符串(`{expr}` 经包装再解析,于当前环境求值格式化)。
- **控制流**:while、`for i in 0..n` / `0..=n`(range 值可先绑再迭代)、跨块遮蔽。
- **函数**:顶层 fn/递归;UFCS(`21.double()` 解析为自由函数);`x.as[T]()` 显式转换(截断/回绕,`300.as[U8]()==44`、`3.5.as[I32]()==3`);断言内建 `assert/assert_eq/assert_ne`。
- **运行时**:setjmp/longjmp 单点中止;一文件一 arena;`suite_rt` 按允许表评分。

## 诚实边界(未支持,deferred = 29)

GC 集合/类/Box、Option/Result/Error 链与 `?`、match/模式、闭包、结构体/切片/定长数组、own 块/arena、scope/spawn/Channel/Mutex/Atomic、trait 对象与方法分派、stdweb/ffi 等 → 后续 C4-b(GC/数据结构+match)、C4-c(并发运行时)、C4-d(其余)逐域并入允许表。

状态:✅ C4-a…h(28 文件 62 test 块全过;08_bare 待语料裁定)。
