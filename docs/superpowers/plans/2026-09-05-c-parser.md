# Ctron C 版:C2 解析器实施计划

> **决策上下文**:见 `compiler_c/README.md`。C 版与 Rust 版**相互独立、各自完整实现**,仅共享语言设计与规范 `docs/spec/` v0.5 与语料 `tests/`。

**Goal:** `compiler_c/` 内独立实现规范 §1.7 EBNF 全语法递归下降解析器(含 §1.8 消歧与 §4 优先级),产出纯数据 AST;61 文件语料分类验收:仅 `01c_parse.neg.ct` 报 E1001、`06_static_var.neg.ct` 报 E3030,其余零解析诊断。行为同源于共享规范与语料验收(与 Rust 版 P1-B 同一验收口径;实现全新)。

**验收:** `make test` 全绿(词法 58 断言 + 61 文件零词法诊断 + 解析单元 + suite_parse 61 文件分类);ASan/UBSan 全绿;`ctronc parse <file> [--ast]` AST 文本确定;2000 层 `(`/1500 层类型括号不栈溢出(嵌套上限 256 → E1001)。

## 任务拆分

- [x] `ast.h`:C 版纯数据 AST(decl/type/expr/block/stmt/pattern 判别式结构 + arena 数组)。
- [x] 声明层:use 组(拆全路径)/ struct/class/enum/trait/impl/const/static(E3030 恢复)/test/fn(vis/comptime/extern ABI/属性/type params/bounds/receiver)。
- [x] 类型:引号/切片/定长数组/Optional/元组/函数类型/SelfT/泛型值实参;`[ ]` 三态消歧(空=切片、单整型=定长数组、其余=泛型实参)。
- [x] 表达式:§4 优先级链(or/&&/比较不可链/range/加减/乘除/一元/后缀);Call/Index/Member/TypeArgs(配对 `]` 后 `(`|`{` 快速路径 + 顶层逗号回退)/Try/闭包/scope/own/if(else 同行检查)/match/块;嵌套上限 256。
- [x] 块与语句:let/var(模式+类型标注)/return/for/while/赋值(目标校验)/表达式语句与块尾表达式。
- [x] 模式:通配/字面量/元组/聚合(路径 + Unit/Tuple/Struct;PascalCase 单段)。
- [x] `ast_show.c`:AST → 确定性 Debug 文本(自举差分产物形态,C-AST v1)。
- [x] CLI `parse [--ast]`、suite_parse(61 文件分类)、单元锚点、ASan/UBSan。

## 过程中未改动

Rust 版 `compiler/`、共享规范与语料。行为参照仅用于对齐验收口径。

状态:✅ 完成(C2)。
