# Ctron C 版:C1 词法器实施计划

> **决策上下文**:见 `compiler_c/README.md` §1。C 版与 Rust 版**相互独立、各自完整实现**,仅共享语言设计与规范 `docs/spec/` v0.5 与语料 `tests/`。本计划按单里程碑执行(与 P1-A 词法同源不同宿主)。

**Goal:** `compiler_c/` 内独立实现规范 §1 词法(记号模型、换行过滤、E1001 诊断、循环错误恢复);61 文件语料在**词法层**零诊断(解析层分类留待 C2)。

**验收:** `make test` 全绿(58 单元断言 + suite_lex 61 文件零诊断);ASan/UBSan 全绿;100k `;` 不栈溢出;CLI `ctronc lex` 位置/码/消息正确。

## 任务拆分(批量执行:一次做完再统一验收)

- [x] 工程骨架:Makefile(`cc`,C11,零依赖)、arena 块链分配器、token 模型、CLI 骨架。
- [x] 词法器核心:名字/关键字/`or` 成员位/`_`、数字(进制小写/后缀小写/浮点消歧/指数)、字符串(转义/`\u{HEX}` 溢出与代理区/插值单行不跨串/裸 `{` E1001)、运算符标点最长匹配(`..=`)、`::`/`;`/未知字符 E1001 与哨兵恢复。
- [x] §1.6 换行显著性过滤:延续集/行首延续集/连续换行折叠/前导 K-1。
- [x] 单元测试 18 组(test_lex.c,58 断言)+ 集成 suite_lex(遍历 `../tests`)。
- [x] 体检:ASan/UBSan、CLI 冒烟、100k 哨兵压力。

## 遗留与后续

- C2(解析器):§1.7 全语法 + AST 文本;`ctronc parse <file> [--ast]`;61 文件分类验收(01c→E1001、06_static_var→E3030、其余零诊断)。`else` 同行等 P1-B 裁决届时并入。
- 过程中未改动 Rust 版与共享规范/语料。

状态:✅ 完成(C1)。
