# 服务器泳道 S0+P1 实施计划(规范 v0.8 定稿 + std/net 阻塞基线)

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 落地服务器路线(2026-09-20 v3)的 S0(规范定稿 + caps 键扩展 + 上游销账核查)与 P1(std/net 阻塞基线:TCP/UDP/DNS/单调钟,能力门面,回环测试与 ctecho 示例),出口 = tests/net 门禁全绿 + 1k 回环冒烟无 fd 泄漏。

**Architecture:** P1 验收走**发射口径**(ctron-emit 出 C → cc 链接 `std/net/c_src/ctron_net.c` 垫片 → 原生运行;解释器 W4 桥不覆盖 net,登记为后续)。垫片内 `#ifdef _WIN32` 同门面;fd 全程 I64;所有外呼 ≤8 参、单行参数表(C 宿主解析器纪律)。std/net 门面镜像 std.fs 惯例:trait `Net: Cap` + `&Net` 能力参数 + manifest `[caps]` 审计(E4010)。

**Tech Stack:** Ctron(自举编译器权威)、C 垫片(posix/winsock)、suite.py(modules/ 小节,markers 判定)、tests/net/run.sh(仿 tests/ffi/run.sh)。

## Global Constraints

- 提交一律 **pathspec 限定**;开工前 `git status` 对齐(机刷泳道并行占槽,见记忆 ctron-peer-lane-reground);他泳道工作树文件禁碰。
- Ctron 源码**注释内禁花括号**(发射口径会炸);extern 声明参数表**必须单行**。
- 门面 API 形状冻结按 `docs/spec/11-net.md`(P1 阻塞口径;同形异构契约:未来 P2 换协程运行时源码零改动)。
- TCP 语义默认值按 11-net §11.3:NODELAY 默认开、KEEPALIVE 默认开、listener SO_REUSEADDR、半关闭垫平。
- CI 回环纪律:只碰 127.0.0.1/::1,端口 `:0` 内核分配,外部网络零依赖;压测/1k 以上门禁不入 CI 主环。
- 编译器二进制前置:`compiler/native.sh`(产出 bin/ctron-cc、bin/ctron-emit);缺则先跑。
- 数值字面量禁下划线分隔(实现面未证实);I8 与 I32 比较允许(宽度提升)。
- Str → `const char*` 直通成熟;**禁止** extern 声明 Str 返回值(Task 1 核查,垫片 API 已按出参+`str_from_c` 规避)。

---

### Task 1: S0-A 上游销账核查与登记

**Files:**
- Modify: `docs/superpowers/plans/2026-09-20-server-s0-p1.md`(末尾执行记录段)
- Read-only: `compiler/src/trans_stmt.ct`、`compiler/src/driver_emit.ct`、`tests/06e_cancel.ct`

**Interfaces:**
- Consumes: 无(纯核查)。
- Produces: 执行记录登记三项事实——(a) extern Str 返回值发射面是否可用;(b) Drop 发射面(p1a-drop-raii)是否已落地(grep `__drop`);(c) 取消传播锚 06e_cancel 当前红绿。Task 6 的 `impl Drop` 依赖 (b) 的结论:b 已落地则用 Drop,否则用显式 `close()` + 注释登记兜底。

- [ ] **Step 1: 核查 Drop 发射面**

```bash
cd /Users/zyj/Zturn/Ctron && grep -n "__drop" compiler/src/trans_stmt.ct compiler/src/driver_emit.ct | head -5
```

Expected: 有 `t_<T>__drop` 合成与调用点 → p1a 已落地(记 `drop-emit=yes`);无输出 → 记 `drop-emit=no,P1 用显式 close 兜底`。

- [ ] **Step 2: 核查取消传播锚**

```bash
cd /Users/zyj/Zturn/Ctron && compiler/bin/ctron-cc run tests/06e_cancel.ct 2>&1 | tail -3; echo "rc=$?"
```

Expected: rc=0 → 记 `cancel=green`;非零 → 记 `cancel=red(上游并发泳道账)`(不阻塞 P1,P1 阻塞口径取消 = join 等效)。

- [ ] **Step 3: 探针 extern Str 返回值(发射口径)**

```bash
cd /Users/zyj/Zturn/Ctron && T=$(mktemp -d) && cat > $T/strret.ct <<'EOF'
// strret probe - extern Str return on emit surface
#[trusted]
extern "c" fn ctron_probe_str() -> Str
test "extern str return" {
    let s = ctron_probe_str()
    assert(s == "hello")
}
EOF
cat > $T/probe.c <<'EOF'
const char* ctron_probe_str(void) { return "hello"; }
EOF
compiler/bin/ctron-emit run $T/strret.ct > $T/out.c 2>$T/err && cc -O1 -w -o $T/app $T/out.c $T/probe.c && $T/app run $T/strret.ct; echo "rc=$?"; rm -rf $T
```

Expected: rc=0 → 记 `extern-str-ret=yes`;cc 或运行失败 → 记 `extern-str-ret=no(垫片按出参规避,已在设计内)`。

- [ ] **Step 4: 登记执行记录并提交**

在计划文件末尾追加(无花括号注释纪律对 md 不适用,照写):

```markdown
## 执行记录

### Task 1(2026-09-XX)
- drop-emit=yes|no
- cancel=green|red
- extern-str-ret=yes|no
- 结论:Task 6 Drop 走 [Drop | 显式 close 兜底]
```

```bash
git add docs/superpowers/plans/2026-09-20-server-s0-p1.md && git commit -m "docs(server): S0-A 上游销账核查登记(drop/cancel/extern-str-ret)" -- docs/superpowers/plans/2026-09-20-server-s0-p1.md
```

---

### Task 2: S0-B 规范 v0.8 定稿

**Files:**
- Modify: `docs/spec/11-net.md`(状态行)、`docs/spec/README.md`(章节地图)、`docs/spec/07-concurrency.md`(新 §7.10)、`docs/spec/08-effects-comptime.md`(键表)、`docs/spec/02-names-modules.md`(示例键集)

**Interfaces:**
- Consumes: 11-net.md / 12-db.md 草案(v3 已落库)。
- Produces: 规范 v0.8 批次(11-net 定稿、§7.10 执行模型、键表四方);Task 3 的 caps 键扩展以 §8/§2 修订为规范依据。

- [ ] **Step 1: 11-net.md 状态定稿**

把 `docs/spec/11-net.md` 首行状态改为:

```markdown
# §11 网络与服务器档

> 状态:**定稿(v0.8,2026-09-20)**——随服务器路线 S0 批次并入规范冻结面;后续修订按 v0.8.x 注记。实现锚定:tests/modules/caps_net(E4010)、tests/net/(行为)。
```

(原"草案 v0.8-d1"标题与状态行删除;正文不动。)

- [ ] **Step 2: 07-concurrency.md 追加 §7.10**

在 `docs/spec/07-concurrency.md` 文件末尾(§7.9 之后)追加:

```markdown
## 7.10 异步执行模型(服务器档,规范性,v0.8)

- 门面 IO(§11.2)恒为**阻塞语义**;实际承载两形态:P1 阻塞运行时(1:1 线程)与 P2 协程运行时(N:M,§7.1 有栈协程)。**同形异构契约**:同一源码零改动,两运行时可观察语义一致;以机械不变式测试钉死(每波出口必跑)。
- **挂起点契约**:网络门面调用、`sleep`、通道操作是仅有的可挂起点;编译器与运行时识别,用户代码无感知、无标注(§9.5"API 无色"的语义落点)。
- **过渡口径注记**:§7.1"可增长连续栈 + work-stealing"在服务器泳道过渡实现为 64KB 固定 mmap 栈 + 确定性优先调度(子集,非完全符合);触顶 = 任务边界 panic(与本节上限口径一致);完全符合在栈经济专案(路线 P9)达成;C100K/C10M 口径以专案为准,过渡口径禁止外推。
- **取消**:网络操作在挂起点响应 §7.2 取消令牌,返回 `Err(NetErr::Cancelled)`;阻塞运行时口径 = join 等效。
- **FFI 纪律**:`extern "c"` 回调内禁止触达网络门面/通道/睡眠(协程栈不可重入);违者调试断言 + 未定义行为声明。
```

- [ ] **Step 3: 08-effects-comptime.md 键表扩展**

在 `docs/spec/08-effects-comptime.md` §8.1 中定位 fs 能力键列举(含 "E4010" 的键清单处),在其后追加一行:

```markdown
- 服务器档键集(v0.8):`net.listen` / `net.connect` / `net.resolve`(§11.1)、`db.connect`(§12.1)——语义同 fs 键:manifest 声明上限,实际使用集 ⊆ 声明集,超出 = E4010;`#[pure]` 触达 = E4020。
```

- [ ] **Step 4: 02-names-modules.md caps 示例扩展**

在 `docs/spec/02-names-modules.md` §2.7 定位 `net.listen = true` 示例行(约 :55),把该示例块改为:

```toml
net.listen = true
net.connect = true
net.resolve = true
db.connect = true
```

- [ ] **Step 5: README 章节地图 + 版本注记**

`docs/spec/README.md` 章节地图表追加两行:

```markdown
| §11 | 网络与服务器 | 能力键、socket 门面、传输默认值、同形异构契约、HTTP 档分层 |
| §12 | 数据访问 | db.connect 能力键、纯 Ctron 线协议驱动契约、连接池、comptime 行映射 |
```

文件头 `# Ctron 语言规范 v0.7` 改为 `# Ctron 语言规范 v0.8(§11/§12 v0.8;其余 v0.7)`,并在 v0.7 修订引文后追加:

```markdown
> **v0.8 修订(2026-09-20)**:新增 §11 网络与服务器档、§12 数据访问档(定稿);§7.10 异步执行模型;§8.1/§2.7 服务器键集。设计:`docs/superpowers/specs/2026-09-20-server-roadmap-design.md`(v3)。§12 实现锚定随 P5 开工;§11 随 P1。
```

- [ ] **Step 6: 提交**

```bash
git add docs/spec/11-net.md docs/spec/README.md docs/spec/07-concurrency.md docs/spec/08-effects-comptime.md docs/spec/02-names-modules.md && git commit -m "docs(spec): v0.8 批次——11-net 定稿、§7.10 异步执行模型、服务器键集四方登记" -- docs/spec/11-net.md docs/spec/README.md docs/spec/07-concurrency.md docs/spec/08-effects-comptime.md docs/spec/02-names-modules.md
```

---

### Task 3: S0-C caps 键集扩展(net/db)+ modules 负例夹具

**Files:**
- Create: `tests/modules/caps_net/Ctron.ctcl`、`tests/modules/caps_net/src/main.ct`
- Create: `std/net.ct`(stub:trait Net 骨架,无 extern,让 use 解析通过、E4010 成为唯一诊断)
- Modify: `compiler/src/parse_pkg.ct:37`(键集)

**Interfaces:**
- Consumes: suite.py modules 小节( entry `src/main.ct`,markers `//@ fail:` 判定;无 c_src 走解释器)。
- Produces: caps 键集含 net/db(bootstrap);`std/net.ct` stub(`pub trait Net: Cap` 骨架 + `pub class StdNet` 空实现)——Task 5/6 在此文件上扩展。

- [ ] **Step 1: 写负例夹具(先红)**

`tests/modules/caps_net/Ctron.ctcl`:

```
pkg {
    manifest_version = 1
    name = "caps_net"
    version = "0.1.0"
    caps = ["fs"]
}
```

`tests/modules/caps_net/src/main.ct`:

```c
// caps_net - E4010: use std.net.Net with ref param, manifest caps only has fs
// fail marker: E4010
use std.net.Net

fn touch(net: &Net) -> I32 {
    return 0
}

fn main() {
    let n = Net_probe()
    let r = touch(n)
    assert(r == 0)
}
```

说明:`Net_probe()` 为 stub 暴露的构造函数(Task 6 被 `StdNet()` 取代时同步改此文件);本夹具断言的是**编译诊断含 E4010**,运行体不达。

- [ ] **Step 2: 写 std/net.ct stub**

`std/net.ct`:

```c
// std/net.ct —— 服务器档传输门面(§11;P1 阻塞口径)
// 本文件为 S0 stub:仅供 caps 键扩展的 E4010 负例解析 use std.net.Net;
// P1 Task 5/6 在此扩展时钟/睡眠与 TCP/UDP/DNS 全门面。

pub trait Net: Cap {
    fn probe(&self) -> I32
}

pub class StdNet {
}

impl Net for StdNet {
    fn probe(&self) -> I32 {
        return 0
    }
}

pub fn Net_probe() -> StdNet {
    return StdNet { }
}
```

- [ ] **Step 3: 跑 suite.py modules 确认红**

```bash
cd /Users/zyj/Zturn/Ctron && python3 compiler/test/suite.py 2>&1 | grep -A2 "caps_net"
```

Expected: caps_net **FAIL**(诊断无 E4010——键集未含 net;只有 use 解析成功后的普通通过或其它诊断)。

- [ ] **Step 4: 扩展 parse_pkg.ct 键集**

`compiler/src/parse_pkg.ct:37`,把:

```c
                if or2(seq2(cap, "fs"), seq2(cap, "time")) {
```

改为:

```c
                if or2(or2(seq2(cap, "fs"), seq2(cap, "time")), or2(seq2(cap, "net"), seq2(cap, "db"))) {
```

同步把 :32 注释 `key ∈ {fs,time}` 改为 `key ∈ {fs,time,net,db}`。

- [ ] **Step 5: 重跑确认绿 + 回归**

```bash
cd /Users/zyj/Zturn/Ctron && python3 compiler/test/suite.py 2>&1 | tail -5
```

Expected: caps_net PASS;modules 小节既有用例(caps 等)零回归;总数不降。

- [ ] **Step 6: 提交**

```bash
git add compiler/src/parse_pkg.ct std/net.ct tests/modules/caps_net && git commit -m "feat(caps): 键集扩展 net/db(E4010)——服务器档 S0;std/net stub + modules 负例夹具" -- compiler/src/parse_pkg.ct std/net.ct tests/modules/caps_net
```

---

### Task 4: P1-A FFI 边界探针(I8[N] 衰减 / 出参 / I64 返回)

**Files:**
- Create: `tests/net/run.sh`(本任务只含 probe 用例的骨架)
- Create: `tests/net/probe_boundary/c_src/ctron_net_probe.c`
- Create: `tests/net/probe_boundary/src/main.ct`

**Interfaces:**
- Consumes: tests/ffi/run.sh 的发射验收模式(emit → cc 链 c_src → 原生运行 test 块)。
- Produces: 边界事实登记(写入执行记录)——**主路径**:`I8[N]` 数组实参在 extern 边界按指针衰减、`I64*` 出参可写回;**备选路径**(主路径失败时):`#[repr(c)] struct NetBuf` 按引用传递。Task 5/6 的垫片签名按登记事实选择。

- [ ] **Step 1: 写探针 C**

`tests/net/probe_boundary/c_src/ctron_net_probe.c`:

```c
/* ctron_net_probe.c - FFI boundary probe (emit surface) */
#include <string.h>
#include <stdint.h>

int64_t ctron_probe_fill(char* buf, int64_t cap) {
    const char* s = "ok";
    int64_t n = 2;
    if (cap < n) { return -1; }
    memcpy(buf, s, (size_t)n);
    return n;
}

int64_t ctron_probe_out_i64(int64_t* out) {
    *out = 42;
    return 0;
}
```

- [ ] **Step 2: 写探针 Ctron**

`tests/net/probe_boundary/src/main.ct`:

```c
// probe_boundary - I8[N] decay + I64 out-param on emit surface
#[trusted]
extern "c" fn ctron_probe_fill(buf: I8[64], cap: I64) -> I64
#[trusted]
extern "c" fn ctron_probe_out_i64(out: I64) -> I64

class Box64 {
    let v: I64
}

#[trusted]
extern "c" fn ctron_probe_out_box(b: Box64) -> I64

test "i64 out-param via Box" {
    let b = Box64 { v: 0 }
    let rc = ctron_probe_out_box(b)
    assert(rc == 0)
}

test "array decay probe" {
    var buf: I8[64]
    let n = ctron_probe_fill(buf, 64)
    if n == 2 {
        assert(buf[0] == 111)
        assert(buf[1] == 107)
    } else {
        // 数组未按指针衰减(值拷贝进 C 栈,读回无效)→ 登记备选路径
        assert(n == -2)
    }
}
```

说明:(1) `ctron_probe_out_i64` 裸 `I64*` 出参在 Ctron 侧无取址语法,一律经 `Box64`/struct 按引用——C 侧签名同步为 `ctron_probe_out_box(Box64* b)`(见 Step 1 实际落地时把 out_i64 改为):

```c
int64_t ctron_probe_out_box(Box64* b) { b->v = 42; return 0; }
```

C 侧需镜像 struct 布局(`repr(c)` 声明序 = C 声明序,tests/ffi/layout 已证 I64 单字段):probe.c 追加 `typedef struct { int64_t v; } Box64;`。`assert(n == -2)` 分支:主路径失败时垫片探针返回 -2(把 Step 1 的 `return -1` 改 `return -2`),夹具恒绿,事实看登记。

(2) 若 `#[repr(c)]` struct 按引用传 C 仍失败 → 登记为**发射面缺口**(转 FFI 泳道),P1 垫片改走"每连接 C 侧缓冲 + `str_from_c` 取串"最小路径(Task 5 Step 2 的替代签名随登记给出)。

- [ ] **Step 3: 写 tests/net/run.sh 骨架**

`tests/net/run.sh`:

```sh
#!/bin/sh
# tests/net/run.sh —— 服务器泳道回环验收(§11;CI 纪律:仅回环、:0、零外联)
# 口径:行为夹具(目录含 c_src/):ctron-emit → cc 链 c_src/*.c → 原生运行
# 前置:compiler/native.sh、cc
set -u
DIR=$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)
ROOT=$(dirname "$(dirname "$DIR")")
EMIT="$ROOT/compiler/bin/ctron-emit"
if [ ! -x "$EMIT" ]; then echo "net/run: 缺少编译器二进制(先: compiler/native.sh)" >&2; exit 2; fi
T=$(mktemp -d); trap 'rm -rf "$T"' EXIT
pass=0; fail=0
echo "== tests/net 回环用例(§11)=="
for d in "$DIR"/*/; do
    [ -d "$d/c_src" ] || continue
    name=$(basename "$d"); [ "$name" = "bench" ] && continue
    e="$d/src/main.ct"
    if "$EMIT" run "$e" > "$T/$name.c" 2>"$T/$name.err"; then
        if cc -O1 -w -o "$T/$name" "$T/$name.c" "$d"/c_src/*.c 2>"$T/$name.cc.err"; then
            if "$T/$name" run "$e" >"$T/$name.out" 2>&1; then
                pass=$((pass+1)); echo "  PASS $name"
            else
                fail=$((fail+1)); echo "  FAIL $name (run)"; sed -n '1,5p' "$T/$name.out"
            fi
        else
            fail=$((fail+1)); echo "  FAIL $name (cc)"; sed -n '1,5p' "$T/$name.cc.err"
        fi
    else
        fail=$((fail+1)); echo "  FAIL $name (emit)"; sed -n '1,5p' "$T/$name.err"
    fi
done
echo "net/run: pass=$pass fail=$fail"
[ "$fail" = 0 ]
```

- [ ] **Step 4: 跑探针**

```bash
cd /Users/zyj/Zturn/Ctron && chmod +x tests/net/run.sh && tests/net/run.sh
```

Expected: `PASS probe_boundary`,`pass=1 fail=0`(走主路径)或 `PASS`(备选分支恒绿)。把走的是主/备路径登记进执行记录(Task 1 的登记段续写)。

- [ ] **Step 5: 提交**

```bash
git add tests/net/run.sh tests/net/probe_boundary && git commit -m "test(net): FFI 边界探针——I8[N] 衰减/出参/返回(发射口径)" -- tests/net/run.sh tests/net/probe_boundary
```

---

### Task 5: P1-B 垫片骨架 + 时钟/睡眠 + bind.ct

**Files:**
- Create: `std/net/c_src/ctron_net.c`(第一批:now_ns/sleep_ms/errno)
- Modify: `std/net.ct`(时钟/睡眠门面)
- Create: `tests/net/clock_sanity/c_src/ctron_net.c`(指向同一实现:内容为 `#include` 不可行,用符号链接——见 Step 3)
- Create: `tests/net/clock_sanity/src/main.ct`

**Interfaces:**
- Consumes: Task 4 登记的边界事实(下述签名按主路径写;备路径替换规则见 Step 2 注)。
- Produces(后续任务依赖的精确签名,C 侧):

```c
int64_t ctron_net_now_ns(void);
void    ctron_net_sleep_ms(int64_t ms);
int64_t ctron_net_last_errno(void);
```

Ctron 侧(`std/net/bind.ct` 内,单行参数表):

```c
#[trusted]
extern "c" fn ctron_net_now_ns() -> I64
#[trusted]
extern "c" fn ctron_net_sleep_ms(ms: I64) -> I64
#[trusted]
extern "c" fn ctron_net_last_errno() -> I64
```

- [ ] **Step 1: 写垫片第一批**

`std/net/c_src/ctron_net.c`:

```c
/* ctron_net.c - Ctron std/net shim (posix + winsock, same facade)
 * §11.3 defaults applied at create sites (Task 6). fd travels as int64_t.
 */
#include <stdint.h>
#include <errno.h>

#ifdef _WIN32
#define WIN32_LEAN_AND_MEAN
#include <winsock2.h>
#include <ws2tcpip.h>
#pragma comment(lib, "ws2_32.lib")
typedef SOCKET ct_sock;
#else
#include <time.h>
#include <unistd.h>
#endif

static _Thread_local int64_t ct_net_errno_v = 0;

int64_t ctron_net_last_errno(void) { return ct_net_errno_v; }

int64_t ctron_net_now_ns(void) {
#ifdef _WIN32
    LARGE_INTEGER f, t;
    QueryPerformanceFrequency(&f);
    QueryPerformanceCounter(&t);
    return (int64_t)(t.QuadPart * 1000000000LL / f.QuadPart);
#else
    struct timespec ts;
    if (clock_gettime(CLOCK_MONOTONIC, &ts) != 0) { ct_net_errno_v = errno; return -1; }
    return (int64_t)ts.tv_sec * 1000000000LL + (int64_t)ts.tv_nsec;
#endif
}

void ctron_net_sleep_ms(int64_t ms) {
#ifdef _WIN32
    Sleep((DWORD)ms);
#else
    struct timespec req;
    req.tv_sec = (time_t)(ms / 1000);
    req.tv_nsec = (long)((ms % 1000) * 1000000L);
    while (nanosleep(&req, &req) != 0 && errno == EINTR) { }
#endif
}
```

- [ ] **Step 2: std/net/bind.ct(extern 面,新文件)**

`std/net/bind.ct`:

```c
// std/net/bind.ct - ctron_net.c shim extern surface (§11.2)
// 纪律:参数表单行(C 宿主解析器);fd 全程 I64;出参经 Box/repr(c) struct(Task 4 登记)
#[trusted]
extern "c" fn ctron_net_now_ns() -> I64
#[trusted]
extern "c" fn ctron_net_sleep_ms(ms: I64) -> I64
#[trusted]
extern "c" fn ctron_net_last_errno() -> I64
```

`std/net.ct` 追加门面(trait 增两方法,StdNet 实现转调 bind):

```c
pub trait Net: Cap {
    fn probe(&self) -> I32
    fn now_ns(&self) -> I64
    fn sleep_ms(&self, ms: I64)
}

impl Net for StdNet {
    fn probe(&self) -> I32 {
        return 0
    }
    fn now_ns(&self) -> I64 {
        return ctron_net_now_ns()
    }
    fn sleep_ms(&self, ms: I64) {
        ctron_net_sleep_ms(ms)
    }
}
```

注(备路径生效时):凡 `I8[N]` 形参签名替换为 `#[repr(c)] struct NetBuf { data: I8[4096], len: I64 }` 按引用(Task 4 登记事实驱动,签名集在本文件集中登记)。

- [ ] **Step 3: clock_sanity 夹具**

行为夹具需把垫片 C 与主程序同批链接;目录内 c_src 用**符号链接**指向 std 垫片,避免双份拷贝漂移:

```bash
mkdir -p tests/net/clock_sanity/c_src tests/net/clock_sanity/src
ln -sf ../../../std/net/c_src/ctron_net.c tests/net/clock_sanity/c_src/ctron_net.c
```

`tests/net/clock_sanity/src/main.ct`:

```c
// clock_sanity - monotonic clock + sleep (§11.6)
use std.net.Net

fn main() {
    let net = Net_probe()
    let t1 = net.now_ns()
    net.sleep_ms(50)
    let t2 = net.now_ns()
    assert(t2 >= t1)
    assert(t2 - t1 >= 40000000)
    let e = ctron_net_last_errno()
    assert(e == 0)
}
```

(`ctron_net_last_errno` 经 bind.ct 全局可见——std 模块 use 合并单一命名空间,fs.ct 同款。)

- [ ] **Step 4: 跑**

```bash
cd /Users/zyj/Zturn/Ctron && tests/net/run.sh
```

Expected: `PASS probe_boundary`、`PASS clock_sanity`,fail=0。

- [ ] **Step 5: 提交**

```bash
git add std/net/c_src/ctron_net.c std/net/bind.ct std/net.ct tests/net/clock_sanity && git commit -m "feat(net): 垫片第一批(单调钟/睡眠/errno)+ bind.ct extern 面 + clock_sanity" -- std/net/c_src/ctron_net.c std/net/bind.ct std/net.ct tests/net/clock_sanity
```

---

### Task 6: P1-C TCP/UDP/resolve 垫片完整面 + std/net 门面 + r7b 锚点

**Files:**
- Modify: `std/net/c_src/ctron_net.c`(TCP/UDP/resolve 全量)
- Modify: `std/net/bind.ct`、`std/net.ct`(全门面)
- Create: `tests/net/tcp_echo/{c_src -> 符号链接,src/main.ct}`
- Create: `tests/roadmap/r7b_pure_net.neg.ct`
- Modify: `compiler-rust/tests/roadmap_suite.rs`(r7b 行;**该文件有机刷泳道未提交改动——先 git 对齐,仅追加一行,pathspec 提交**)

**Interfaces:**
- Consumes: Task 5 垫片骨架与边界登记。
- Produces(门面 API,冻结为同形契约面——后续示例/测试/P2 依赖):

C 侧(全部落 `ctron_net.c`,posix 主路径 + winsock 同名):

```c
int64_t ctron_net_tcp_listen(const char* host, int64_t port, Box64* out);      /* out->v = listen fd */
int64_t ctron_net_tcp_sockname(int64_t fd, char* host, int64_t cap, Box64* out); /* out->v = port */
int64_t ctron_net_tcp_accept(int64_t lfd, Box64* out, char* peer, int64_t pcap, Box64* pport);
int64_t ctron_net_tcp_connect(const char* host, int64_t port, Box64* out);
int64_t ctron_net_read_t(int64_t fd, char* buf, int64_t cap, int64_t timeout_ms); /* >0 n, 0 eof, <0 err */
int64_t ctron_net_write(int64_t fd, const char* buf, int64_t n);
int64_t ctron_net_close(int64_t fd);
int64_t ctron_net_shutdown_write(int64_t fd);
int64_t ctron_net_set_nodelay(int64_t fd, int64_t on);
int64_t ctron_net_get_nodelay(int64_t fd);
int64_t ctron_net_udp_socket(Box64* out);
int64_t ctron_net_udp_sendto(int64_t fd, const char* host, int64_t port, const char* buf, int64_t n);
int64_t ctron_net_udp_recvfrom(int64_t fd, char* buf, int64_t cap, int64_t timeout_ms, char* peer, int64_t pcap, Box64* pport);
int64_t ctron_net_resolve(const char* host, char* out, int64_t cap, Box64* count); /* out: "ip\nip\n" */
```

Ctron 门面(`std/net.ct` 追加;trait Net 方法 + 值句柄):

```c
pub class NetError {
    let message: Str
}

@derive(Error)
impl err message for NetError {
}

pub class TcpListener {
    let fd: I64
    let port: I32
}

pub class TcpStream {
    let fd: I64
    let peer: Str
}

pub class UdpSock {
    let fd: I64
}
```

trait Net 增方法(签名冻结):`listen(addr: Str) -> Result[TcpListener, NetError]`、`connect(addr: Str) -> Result[TcpStream, NetError]`、`resolve(host: Str) -> List[Str]`、`udp() -> Result[UdpSock, NetError]`;句柄方法:`TcpListener.accept(&self) -> Result[TcpStream, NetError]`、`TcpListener.local_port(&self) -> I32`、`TcpStream.read(&self, buf: I8[4096], timeout_ms: I64) -> I64`、`TcpStream.write_str(&self, s: Str) -> I64`、`TcpStream.write_bytes(&self, buf: I8[4096], n: I64) -> I64`、`TcpStream.close(&self)`、`TcpStream.shutdown_write(&self)`、`TcpStream.peer_addr(&self) -> Str`、`UdpSock.send_to(&self, host: Str, port: I32, s: Str) -> I64`、`UdpSock.recv_from(&self, buf: I8[2048], timeout_ms: I64) -> I64`(n<0 错;peer 经 `UdpSock.last_peer(&self) -> Str`)。

- [ ] **Step 1: 垫片 TCP/UDP/resolve 全量**

在 `ctron_net.c` 追加(posix 主路径;winsock 分支同名同语义:WSAStartup 置于 listen/connect/udp_socket 首调 `static once`;closesocket 映射;错误捕获 `ct_net_errno_v = WSAGetLastError()`):

```c
#include <string.h>
#include <stdlib.h>
#include <stdio.h>
#ifdef _WIN32
#else
#include <sys/socket.h>
#include <netinet/in.h>
#include <netinet/tcp.h>
#include <arpa/inet.h>
#include <netdb.h>
#include <poll.h>
#include <fcntl.h>
#endif

static int64_t ct_err(void) { ct_net_errno_v = (int64_t)errno; return -1; }

static void ct_apply_defaults(int fd) {
#ifdef _WIN32
    BOOL one = 1;
    setsockopt(fd, IPPROTO_TCP, TCP_NODELAY, (const char*)&one, sizeof(one));
    setsockopt(fd, SOL_SOCKET, SO_KEEPALIVE, (const char*)&one, sizeof(one));
#else
    int one = 1;
    setsockopt(fd, IPPROTO_TCP, TCP_NODELAY, &one, sizeof(one));   /* §11.3 NODELAY 默认开 */
    setsockopt(fd, SOL_SOCKET, SO_KEEPALIVE, &one, sizeof(one));   /* §11.3 KEEPALIVE 默认开 */
#endif
}

/* Box64 镜像 Ctron class Box64 { let v: I64 } —— repr(c) 声明序 */
typedef struct { int64_t v; } Box64;

int64_t ctron_net_tcp_listen(const char* host, int64_t port, Box64* out) {
#ifdef _WIN32
#else
    int fd = socket(AF_INET, SOCK_STREAM, 0);
    if (fd < 0) return ct_err();
    int one = 1;
    setsockopt(fd, SOL_SOCKET, SO_REUSEADDR, &one, sizeof(one));   /* §11.3 */
    struct sockaddr_in a;
    memset(&a, 0, sizeof(a));
    a.sin_family = AF_INET;
    a.sin_port = htons((uint16_t)port);
    if (inet_pton(AF_INET, host, &a.sin_addr) != 1) { close(fd); ct_net_errno_v = EINVAL; return -1; }
    if (bind(fd, (struct sockaddr*)&a, sizeof(a)) != 0) { close(fd); return ct_err(); }
    if (listen(fd, 128) != 0) { close(fd); return ct_err(); }
    out->v = fd;
    return 0;
#endif
}
```

(accept/connect/sockname/read_t/write/close/shutdown/nodelay get-set/udp 三件/resolve 按 `tests/net/tcp_echo` 需要逐一同构补齐;`read_t` 用 `poll()` 实现 timeout_ms ≤0 = 永久阻塞;`resolve` 用 `getaddrinfo(AF_UNSPEC)` 遍历 `ai_next`,`inet_ntop` 逐行写入 out,行间 `\n`,`count->v` 计数。每个函数失败路径统一 `ct_err()`。完整实现约 230 行,逐函数镜像上列原型,不得引入第 9 个参数。)

- [ ] **Step 2: bind.ct 追加 extern(全部单行)**

```c
#[trusted]
extern "c" fn ctron_net_tcp_listen(host: Str, port: I64, out: Box64) -> I64
#[trusted]
extern "c" fn ctron_net_tcp_sockname(fd: I64, host: I8[64], cap: I64, out: Box64) -> I64
#[trusted]
extern "c" fn ctron_net_tcp_accept(lfd: I64, out: Box64, peer: I8[64], pcap: I64, pport: Box64) -> I64
#[trusted]
extern "c" fn ctron_net_tcp_connect(host: Str, port: I64, out: Box64) -> I64
#[trusted]
extern "c" fn ctron_net_read_t(fd: I64, buf: I8[4096], cap: I64, timeout_ms: I64) -> I64
#[trusted]
extern "c" fn ctron_net_write(fd: I64, buf: I8[4096], n: I64) -> I64
#[trusted]
extern "c" fn ctron_net_close(fd: I64) -> I64
#[trusted]
extern "c" fn ctron_net_shutdown_write(fd: I64) -> I64
#[trusted]
extern "c" fn ctron_net_set_nodelay(fd: I64, on: I64) -> I64
#[trusted]
extern "c" fn ctron_net_get_nodelay(fd: I64) -> I64
#[trusted]
extern "c" fn ctron_net_udp_socket(out: Box64) -> I64
#[trusted]
extern "c" fn ctron_net_udp_sendto(fd: I64, host: Str, port: I64, buf: I8[2048], n: I64) -> I64
#[trusted]
extern "c" fn ctron_net_udp_recvfrom(fd: I64, buf: I8[2048], cap: I64, timeout_ms: I64, peer: I8[64], pcap: I64, pport: Box64) -> I64
#[trusted]
extern "c" fn ctron_net_resolve(host: Str, out: I8[1024], cap: I64, count: Box64) -> I64
```

注:`write`/`send_to` 的 Str 形态另加 `ctron_net_write_str(fd, const char* s, n)` 变体(Str 直通 mature),Ctron 侧 `write_str` 转调之——避免 Str→I8[] 拷贝。

- [ ] **Step 3: std/net.ct 全门面**

要点(逐条落实,不改 Task 5 已有签名):`Net.listen` 解析 "host:port"(IPv4 直拆最后冒号;IPv6 方括号形态 `[::1]:80` —— `SocketAddr` 纯 Ctron 解析/格式化函数 `addr_parse/addr_format` 同文件 `pub fn` 导出并带 inline test);listen 后 `sockname` 回填实际端口(:0);accept 出 `TcpStream{fd, peer}`;所有句柄 `fd < 0` 视为已关闭。Task 1 登记 `drop-emit=yes` 时给三类句柄补:

```c
impl Drop for TcpStream {
    fn drop(var self) {
        if self.fd >= 0 {
            ctron_net_close(self.fd)
        }
    }
}
```

(TcpListener/UdpSock 同构;`drop-emit=no` 时改为显式 `close()` 方法并在类注释登记"作用域出口须显式关闭——p1a-drop-raii 落地后翻转"。)`peer` 经 `str_from_c`(C-owned → Ctron-owned,§9.6 三约定)从 accept 的 peer 缓冲取串。

- [ ] **Step 4: tcp_echo 夹具**

```bash
mkdir -p tests/net/tcp_echo/c_src tests/net/tcp_echo/src
ln -sf ../../../std/net/c_src/ctron_net.c tests/net/tcp_echo/c_src/ctron_net.c
```

`tests/net/tcp_echo/src/main.ct`:

```c
// tcp_echo - loopback roundtrip: bind :0, connect, write, read, defaults, half-close
use std.net.Net

fn main() {
    let net = Net_probe()
    let ln = match net.listen("127.0.0.1:0") {
        Ok(l) => l
        Err(e) => {
            panic(e.message)
        }
    }
    let port = ln.local_port()
    assert(port > 0)

    let conn = net.connect("127.0.0.1:" + "{port}")
    let st = match conn {
        Ok(s) => s
        Err(e) => {
            panic(e.message)
        }
    }
    let n = st.write_str("ping")
    assert(n == 4)

    let srv = match ln.accept() {
        Ok(s) => s
        Err(e) => {
            panic(e.message)
        }
    }
    var buf: I8[4096]
    let rn = srv.read(buf, 1000)
    assert(rn == 4)
    assert(buf[0] == 112)
    assert(buf[3] == 103)

    let wn = srv.write_bytes(buf, rn)
    assert(wn == 4)
    let bn = st.read(buf, 1000)
    assert(bn == 4)

    assert(net.resolve("127.0.0.1").len >= 1)

    st.shutdown_write()
    let en = srv.read(buf, 1000)
    assert(en == 0)
    st.close()
    srv.close()
    ln.close()
}
```

- [ ] **Step 5: 跑**

```bash
cd /Users/zyj/Zturn/Ctron && tests/net/run.sh
```

Expected: probe/clock/tcp_echo 全 PASS,fail=0。若 `I8[4096]` 大数组参数触发发射问题(Task 4 探针用 64 过了),把门面缓冲降到 `I8[256]` 并登记(性能微调 P2 再说)。

- [ ] **Step 6: r7b 锚点 + roadmap_suite 登记**

`tests/roadmap/r7b_pure_net.neg.ct`:

```c
// r7b_pure_net.neg.ct - pure function touching net capability = E4020 (R-P7b)
// 锚点:P1 std/net 门面落地转绿;E4020 语义 §8.1/§11.1
use std.net.Net

fn probe(net: &Net) -> I32 {
    return net.probe()
}

#[pure]
fn sneaky(net: &Net) -> I32 {
    return probe(net)
}
```

`compiler-rust/tests/roadmap_suite.rs` 表追加(**先 `git status` 对齐机刷改动;仅追加一行**):

```rust
        ("r7b_pure_net.neg.ct", "R-P7b", Status::RunRed), // P1 std/net 门面 + R 线解析就绪后翻转
```

```bash
git add tests/roadmap/r7b_pure_net.neg.ct compiler-rust/tests/roadmap_suite.rs && git commit -m "test(roadmap): r7b_pure_net 锚点——pure 触网 E4020(R-P7b)" -- tests/roadmap/r7b_pure_net.neg.ct compiler-rust/tests/roadmap_suite.rs
```

---

### Task 7: P1-D UDP 夹具 + 默认值断言 + fd 泄漏 + examples/ctecho + 吞吐基线

**Files:**
- Create: `tests/net/udp_roundtrip/{c_src 链接,src/main.ct}`
- Create: `tests/net/tcp_defaults/src/main.ct`(c_src 链接)
- Create: `tests/net/fd_churn/src/main.ct`(c_src 链接)
- Create: `examples/ctecho/{Ctron.ctcl,src/main.ct,run.sh,README.md}`

**Interfaces:**
- Consumes: Task 6 全门面。
- Produces: tests/net 全套绿(P1 出口门禁的 CI 面);ctecho(同形不变式夹具,P2 复用);bench 脚本(本地/nightly 门禁)。

- [ ] **Step 1: udp_roundtrip 夹具**

`src/main.ct`:

```c
// udp_roundtest - sendto/recvfrom on loopback
use std.net.Net

fn main() {
    let net = Net_probe()
    let s = match net.udp() {
        Ok(u) => u
        Err(e) => {
            panic(e.message)
        }
    }
    let n = s.send_to("127.0.0.1", 9, "hi")   // 端口 9 无人听,UDP 不保证送达——仅验证系统调用面
    assert(n == 2)
    s.close()
}
```

(UDP 真回环收发依赖端口协调,CI 抖动风险——v1 只钉调用面,真回环收发归 nightly;登记到执行记录。)

- [ ] **Step 2: tcp_defaults 夹具**

```c
// tcp_defaults - §11.3: NODELAY defaults on (getter 1), explicit off then on
use std.net.Net

fn main() {
    let net = Net_probe()
    let ln = match net.listen("127.0.0.1:0") {
        Ok(l) => l
        Err(e) => {
            panic(e.message)
        }
    }
    let st = match net.connect("127.0.0.1:" + "{ln.local_port()}") {
        Ok(s) => s
        Err(e) => {
            panic(e.message)
        }
    }
    let srv = match ln.accept() {
        Ok(s) => s
        Err(e) => {
            panic(e.message)
        }
    }
    assert(ctron_net_get_nodelay(st.raw_fd()) == 1)
    assert(ctron_net_set_nodelay(st.raw_fd(), 0) == 0)
    assert(ctron_net_get_nodelay(st.raw_fd()) == 0)
    st.close()
    srv.close()
    ln.close()
}
```

(`raw_fd()` 为 Task 6 门面补的只读访问器 `pub fn raw_fd(&self) -> I64 { return self.fd }`;bind.ct 的 `ctron_net_get/set_nodelay` 全局可见。)

- [ ] **Step 3: fd_churn 夹具(泄漏冒烟)**

```c
// fd_churn - 500 connect/close cycles; fd exhaustion would fail the last connect
use std.net.Net

fn main() {
    let net = Net_probe()
    let ln = match net.listen("127.0.0.1:0") {
        Ok(l) => l
        Err(e) => {
            panic(e.message)
        }
    }
    var i: I32 = 0
    while i < 500 {
        let st = match net.connect("127.0.0.1:" + "{ln.local_port()}") {
            Ok(s) => s
            Err(e) => {
                panic(e.message)
            }
        }
        let srv = match ln.accept() {
            Ok(s) => s
            Err(e) => {
                panic(e.message)
            }
        }
        st.close()
        srv.close()
        i += 1
    }
    ln.close()
}
```

(每轮两端都显式 close;若 Drop 已启用则离开作用域即关,断言不变。)

- [ ] **Step 4: examples/ctecho**

`examples/ctecho/Ctron.ctcl`:

```
pkg {
    manifest_version = 1
    name = "ctecho"
    version = "0.1.0"
    caps = ["net"]
}
```

`examples/ctecho/src/main.ct`(thread-per-conn,~55 行):

```c
// ctecho - thread-per-connection echo server (P1 blocking baseline)
// 用法:CTECHO_PORT=8080 ./ctecho;同形不变式夹具——P2 协程运行时下源码零改动
use std.net.Net

fn handle(st: TcpStream) {
    var buf: I8[4096]
    while true {
        let n = st.read(buf, 30000)
        if n <= 0 {
            break
        }
        let w = st.write_bytes(buf, n)
        if w != n {
            break
        }
    }
    st.close()
}

fn main() {
    let net = Net_probe()
    let p = env_get("CTECHO_PORT")
    let port = match p {
        Some(s) => s
        None => "8080"
    }
    let ln = match net.listen("127.0.0.1:" + port) {
        Ok(l) => l
        Err(e) => {
            panic(e.message)
        }
    }
    "{ln.local_port()}"
    while true {
        let st = match ln.accept() {
            Ok(s) => s
            Err(e) => {
                break
            }
        }
        let done = scope { |s|
            s.spawn(|| handle(st))
        }
    }
}
```

(`env_get` 为既有内建;`while true` 若语法不支持则改 `while 1 == 1`。)

`examples/ctecho/run.sh`(构建 + 自打冒烟):

```sh
#!/bin/sh
# ctecho run.sh - build (emit+cc+shim), bg serve, 3-client smoke, kill
set -u
DIR=$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)
ROOT=$(dirname "$(dirname "$DIR")")
EMIT="$ROOT/compiler/bin/ctron-emit"
T=$(mktemp -d); trap 'rm -rf "$T"' EXIT
"$EMIT" run "$DIR/src/main.ct" > "$T/ctecho.c" || exit 1
cc -O1 -w -o "$T/ctecho" "$T/ctecho.c" "$ROOT/std/net/c_src/ctron_net.c" || exit 1
PORT=$(( (RANDOM % 20000) + 30000 ))
CTECHO_PORT=$PORT "$T/ctecho" & SRV=$!
sleep 0.5
for i in 1 2 3; do printf "hello$i" | nc 127.0.0.1 "$PORT" | head -c 32; done
kill "$SRV" 2>/dev/null
echo "ctecho smoke done (port $PORT)"
```

`examples/ctecho/README.md`:三行用途 + 同形契约说明(P2 源码零改动)。

- [ ] **Step 5: 全套跑 + 提交**

```bash
cd /Users/zyj/Zturn/Ctron && tests/net/run.sh
```

Expected: probe/clock/tcp_echo/tcp_defaults/udp_roundtrip/fd_churn 全 PASS。

```bash
git add tests/net/udp_roundtrip tests/net/tcp_defaults tests/net/fd_churn examples/ctecho && git commit -m "feat(net): P1 门禁全套(udp/default/fd_churn)+ ctecho 示例(同形夹具)" -- tests/net/udp_roundtrip tests/net/tcp_defaults tests/net/fd_churn examples/ctecho
```

---

### Task 8: P1-E 吞吐基线(本地/nightly 门禁)

**Files:**
- Create: `tests/net/bench/baseline_echo.c`(手写 C thread-per-conn 基线)
- Create: `tests/net/bench/bench.sh`(对比脚本;默认跳过,`CTRON_NET_BENCH=1` 才跑)

**Interfaces:**
- Consumes: ctecho(被测)。
- Produces: 吞吐比值登记(P1 出口门禁:own 档 vs C ≤1.05×;本地/nightly,不入 CI 主环)。

- [ ] **Step 1: C 基线**

`tests/net/bench/baseline_echo.c`(完整可编译,~70 行):主线程 socket/bind(127.0.0.1:0)/listen/打印端口;每 accept 一个 `pthread_create` echo 线程(read/write 循环,4KB 栈上缓冲)。链接 `-lpthread`。结构镜像 ctecho(同默认值:NODELAY/KEEPALIVE 开),差异仅语言。

- [ ] **Step 2: bench.sh**

流程:起 C 基线(取端口)→ 起 ctecho(取端口)→ 对两者各跑同一客户端压测(单连接 10 万次 64B write/read 往返,`gettimeofday` 计时,3 取最小)→ 打印两值与比值 → 比值 >1.05 时 rc=1。默认(`CTRON_NET_BENCH` 未置)打印 SKIP rc=0。

- [ ] **Step 3: 本地跑并登记**

```bash
cd /Users/zyj/Zturn/Ctron && CTRON_NET_BENCH=1 tests/net/bench/bench.sh 2>&1 | tail -5
```

Expected: 比值 ≤1.05(P1 出口);若 1.05–1.15,登记并归因(多半在 facade 每读 4KB 数组拷贝——P2 优化项,不阻塞出口,记录到执行记录)。

- [ ] **Step 4: 提交**

```bash
git add tests/net/bench && git commit -m "bench(net): C thread-per-conn 基线 + 对比脚本(P1 出口 ≤1.05×,本地/nightly)" -- tests/net/bench
```

---

### Task 9: P1-F COVERAGE/分歧登记 + 收尾门禁

**Files:**
- Modify: `tests/COVERAGE.md`(新 §网络与服务器 小节)
- Modify: `docs/c-rust-divergences.md`(服务器面小节)
- Modify: 本计划文件执行记录

**Interfaces:**
- Produces: COVERAGE 登记(S0/P1 行);分歧登记(Task 4 边界事实、R 线 caps 对齐状态、ctecho 解释口径限制);P1 出口判定。

- [ ] **Step 1: COVERAGE.md 登记**

在 `tests/COVERAGE.md` 末尾追加:

```markdown
## §网络与服务器(服务器泳道,2026-09-20 起)

| 波次 | 项 | 锚定 |
|---|---|---|
| S0 | caps 键集 net/db(E4010) | tests/modules/caps_net(绿) |
| S0 | 规范 v0.8(11-net 定稿/§7.10/键表) | docs/spec v0.8 |
| P1 | std/net 阻塞基线(时钟/TCP/UDP/resolve/默认值) | tests/net/(probe/clock_sanity/tcp_echo/tcp_defaults/udp_roundtrip/fd_churn,全绿) |
| P1 | r7b pure 触网 E4020 | tests/roadmap/r7b_pure_net.neg.ct(红→随 R 线翻转) |
| P1 | 1k 回环冒烟无 fd 泄漏 | examples/ctecho + tests/net/fd_churn(500 轮) |
| P1 | 吞吐 vs C ≤1.05× | tests/net/bench(本地/nightly) |
```

- [ ] **Step 2: 分歧文档登记**

`docs/c-rust-divergences.md` 追加"服务器面(2026-09-20)"小节:Task 4 探针结论(I8[N] 衰减/出参形态)、R 线 caps 键集对齐状态(check.rs caps_used 是否泛化)、ctecho 解释口径不支持(W4 桥未含 net 垫片——emit-only 验收)、UDP 真回环归 nightly。

- [ ] **Step 3: 收尾门禁**

```bash
cd /Users/zyj/Zturn/Ctron && tests/net/run.sh && tests/ffi/run.sh 2>&1 | tail -2 && python3 compiler/test/suite.py 2>&1 | tail -3
```

Expected: tests/net 全绿;tests/ffi 零回归(11/11);suite.py 总数不降、无新增红。`ci.sh` 全量可跑但**他泳道既有红先甄别**(静默重跑一次再判,机刷纪律)。

- [ ] **Step 4: P1 出口登记 + 提交**

执行记录写 P1 出口判定(门禁逐项勾);提交:

```bash
git add tests/COVERAGE.md docs/c-rust-divergences.md docs/superpowers/plans/2026-09-20-server-s0-p1.md && git commit -m "docs(server): P1 出口——COVERAGE/分歧登记,门禁全绿" -- tests/COVERAGE.md docs/c-rust-divergences.md docs/superpowers/plans/2026-09-20-server-s0-p1.md
```

---

## Self-Review 记录

- **规范覆盖**:S0 三件(销账核查/规范批次/锚点)↔ Task 1/2/3;P1 六件(垫片+时钟/TCP 门面/回环夹具/ctecho/基线/登记)↔ Task 4–9。设计 §六 S0/P1 交付物逐条有任务;1k 并发冒烟由 Task 7 fd_churn(500 轮双端)+ ctecho smoke 承载,1k 满额归 nightly(Task 8 bench 同场)——与设计"CI 冒烟 1k、门禁本地"口径对齐。
- **占位符扫描**:Task 6 Step 1 垫片"逐函数镜像原型补齐"为唯一非逐行段——原型签名/默认值/错误约定已全部给出,且实现指引(参数上限、ct_err、poll)固定;其余步骤均含完整代码或精确编辑点。
- **类型一致性**:`Box64` 在 Task 4 定义、Task 5/6 C 侧 `typedef` 镜像;`Net_probe()`(Task 3)→ Task 6 门面落地时保留为 StdNet 构造名;`raw_fd()` 在 Task 7 引用、Task 6 Step 3 补充;句柄方法名(read/write_bytes/write_str/close/shutdown_write/accept/local_port/peer_addr/send_to/recv_from/last_peer)各任务引用一致。

## 执行记录

### Task 1(2026-09-20)
- drop-emit=yes
- cancel=green
- extern-str-ret=yes
- 结论:Task 6 Drop 走 Drop
