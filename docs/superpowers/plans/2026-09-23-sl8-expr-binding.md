# SL-8 (L2-3) 切片计划：{expr} 绑定 + on: 自动闭包 + 钩子退役

> 2026-09-23。前置 P0 已收口（d73afcc，骨架树判据正确）。设计源：use-gui §5.1/§9
> SL-8 行、gui-ctml §4.2 要点 2′/3、§4.4 E8190。本计划把 SL-8 拆为三个独立验收
> 子片，每片全绿即落库。

## 架构定音（本计划裁决）

1. **规范形 = 去空白原文**：`{...}` 槽内容在所有面（编译骨架/check/域包树/运行时
   查询名）统一存储为**去除全部空白后的表达式原文**（字符串字面量内空白保留）。
   该形对两侧词法粒度不变——W1 按 token 空格拼接与域包按字节原始捕获在剥空白后
   必然同形（如 `{a + b}`→`a+b`、`{user.name}`→`user.name`），s22 双端差分可证。
2. **执行架构分层**：8a 只动解析/检查/存储（运行时中性）；8b 域包求值器（受限
   纯读解释：字面量/标识符/点链/算术比较/len，环境 = 现有 bind 名通道 + 新值环境）；
   8c 编译器 on: 自动闭包（emit 面生成事件闭包，钩子/锚退役）。
   编译期自动闭包是 L2 正统（设计 §5.1），域包解释执行框架内 on: 先走
   "表达式→动作名+实参字面量"的受限形态，8c 收编。
3. **E8190 纯读门 = check 面静态走查**（赋值/复合赋值/自增减 token 形态即拒；
   on:/fn props 槽豁免）；可变方法调用的深度判定随 8c 类型面补强，8a 先拦
   token 可判定形态（语料可测确定性优先）。
4. **运行时兼容**：ident 与点链形槽继续按名查询（现协议零破坏）；含运算符的
   表达式槽在 8b 前由运行时响亮 panic（带原文），不静默空渲染。

## 子片与验收

### 8a 解析 + 纯读门 + 骨架原文 —— ✅ 落库（02e65b8，2026-09-23）
- 编译侧 gui_parse.ct：花括号槽从单词放宽为平衡花括号表达式，规范形 = 去空白
  原文；**四路径统一捕获**——gui_sk_attrs / gui_ck_elem / gui_lower_element
  （真凶：重建路径旧单词捕获对多词内容直接 panic，探针实证后一并收编）/
  each 位置头。E8190 纯读门：赋值/复合赋值/自增减 token 形态即拒，比较与
  字符串豁免，on: 槽天然豁免；已知边界：`a - -b`/`a + +b` 负字面量误报（let 拆解绕行）。
- 域包 std/gui.ct：gt_attrs/each 头/叶插值同规范形捕获；运行时 ident/点链
  照旧名字查询零破坏，含运算符槽响亮 panic 待 8b（原生口径实证）。
- e8 语料 15→19（E8190 neg×内嵌/独立 + 表达式 pos×内嵌/独立）；
  s22 夹具新增 {count > 0}/{form.draft} 双端差分锁定规范形等价。

### 8b 域包求值器 —— ✅ 落库（88f3b7a，2026-09-23）
- bxv_* 递归下降受限纯读求值器：cmp/add/mul/unary/primary 五级文法；值 =
  打标串（i: 整 / b: 布 / 裸 = 串），bind 通道回答逐字面解码带型；路径 .len
  后缀取串长；无调用无赋值。
- rt_emit 接线：cond 槽表达式取真值、叶/bind 槽求值渲染；简单槽零破坏。
- s25_expr_d 入阶梯（阶梯 29）：叶串接（draft + "!"）/比较条件（n > 0）/
  算术渲染（n * 2）/带型通道/动作变更逐帧活值，原生口径全绿。
- 坑位登记：内嵌叶字面量含冒号被重建插空格（已知限制——夹具避标点，根治归
  骨架 IR 槽表直通）；一次 e8 2 败为 peer 构建竞态，复跑即清。

### 8b 域包求值器（下一片）
- 受限纯读表达式求值：字面量/ident（名通道回落）/点链（值环境 + .len）/
  算术/比较；值环境 API（put_i/put_s/put_b/put_items）= 用户按字段枚举模型
  （替代按槽分发，模型暴露胶水下限）。
- s25_expr_d 夹具：{expr} 绑定 + each 项表达式 + 条件表达式，活模型多帧。
- rt_emit 槽求值接线 + 运算符形 panic 退役。

### 8c on: 自动闭包 + 钩子退役（末片）—— ⛔ 已登记阻塞（2026-09-23）
- **阻塞**：需动 trans/emit 面（闭包生成/props 语义），trans_emit.ct 正是 peer
  在制品——按泳道纪律登记阻塞，待 peer 落库后开片。
- 编译器 emit/eval 面：on:={expr} 编译为事件闭包（事件时求值，副作用允许）；
- gui.run 单入口 + view props（§10.3 形态子集）；calc 切表达式事件；
- §10.3 交互 Todo 形态可跑（props/key/checkbox 仍属终锚范围，如实登记差集）。
- **已就位基座（8a/8b 交付）**：规范形表达式槽两侧同形入树；E8190 check 面；
  域包求值器（cond/叶/bind 三槽）；带型通道约定（i:/b:/裸）；e8 语料 19；
  阶梯 29（s24_model_d + s25_expr_d）。8c 动工时 8b 的 bxv_* 可作为解释口径
  事件表达式求值底座复用。

## 泳道外交互发现（转交 use 别名 P1b 泳道，2026-09-23 实证）

**选择性合并（7e8af9c）对"引用但非请求"的 decl 产签名级形态，两口径双症状**：

1. **解释口径 index target 根因实锤**（域包 test() 驱动解释口径整体红的真身）：
   use 合并后的跨包 fn（如 std.gui test）为 **len=6 签名级 stub 节点**
   （`FnPub|test|tsLnorTC…` 四槽皆类型戳、无体槽）——解释口径 call_decl_vals
   落 stub 即 eval_expr Index 炸 "index target"。判定手法：panic 标记法
   （println 缓冲在 panic 时丢失，不可作迹）；实参求值期闭包逐个二分 +
   call fallthrough 节点转储。
2. **原生口径 gui_calc 发射红**：`t_d_press_key` undeclared——calc 引用
   d_press_key 但未入 use 请求面，闭包收集未携带 → 发射面声明缺失。
   gui_cjk/todo/gui_counter 三示例全绿（其引用面恰在请求∪闭包内）。

两症状同根（stub 无体/闭包收集缺口），随 P1b 闭包收集收口应自愈；验收锚：
解释口径 `ctron-cc run` 跑通域包 test() 探针 + `sh examples/gui_calc/run.sh`
复绿。GUI 泳道回归态（阶梯 26/30、e8 19/19、todo/counter/cjk 全绿）不受累。

**更新（2026-09-23 晚，21fef21）**：native 半已关——gui_calc 按 ddb5ee7 先例
（调用方补显式请求）补 `d_press_key` 入 use 面即复绿（rc=0 全场景断言过）。
**选择性合并的现行契约 = 引用跨包符号必须显式请求**（闭包收集有边界，跨包
extern 尤甚）。interp 半（请求面内的 test 仍 stub）仍归 P1b 合并器，登记不变。

## 非目标（本计划不顺手做）
- view 组件/props 类型面/each key/slot 分发（终锚 §10.3 全形态）；
- 可变方法调用的类型级纯度判定（等 8c 类型面）；
- 求值缓存/脏追踪（§12.3c D5：每帧全量重放）。
