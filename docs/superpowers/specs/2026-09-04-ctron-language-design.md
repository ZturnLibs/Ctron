# Ctron 语言设计文档(v0.2)

日期:2026-09-04(v0.2 修订;v0.1 为脑暴稿)
状态:待评审(brainstorm 产出,尚未实现)
决策记录:内存模型 = **GC 默认 + 所有权逃生舱**;性能底线 = **必须匹敌 C/Rust**(均已由用户确认)
v0.2 修订:吸收设计评审结论——补齐数据竞争消除机制(§7.2)、own 块进出语义(§5.3)、分配效果追踪(§5.4)、FFI/`#[trusted]` 诚实化(§3/§5.7)、模块系统与 trait 一致性(§6.5)、引用实现宿主语言(§8.6);修正分段栈表述、推断"必成功"措辞、运行时体积口径、GC 性能指标口径;补 RAII 资源硬规则、struct 别名语义、错误包装、参数可变性、栈溢出、字符串切片等遗漏(§17 修订记录)。

---

## 0. 一句话定位

> **Ctron 是一门为"AI 写、人读"时代设计的系统级语言**:编译器当护栏、效果全显式、一条命令工具链;GC 档保生成顺畅与阅读清爽,`own` 档与裸机档追平 C;同一门语言覆盖服务器、桌面、WebAssembly 与嵌入式。

## 1. 问题定义与需求来源

### 1.1 场景前提

AI 已承担大部分代码**编写**,人类的主要工作转为**阅读、审查、验证**生成的代码。这个前提改变语言设计的优化目标:

| 传统语言优化目标 | Ctron 的目标 |
|---|---|
| 写起来省字符 | 读起来零歧义;AI 写、人读各取所需 |
| 表达力强、灵活 | 可预测性强;一种问题只有一种惯用写法 |
| 错误运行时暴露 | 编译期拦下;AI 生成的代码"能编译 ≈ 大概率对" |
| 语法简洁优先 | tokenizer/LLM 友好优先(显式花括号、可 grep 符号) |

### 1.2 社区真实痛点(2024–2026 调研证据)

| 痛点 | 来源语言 | Ctron 的回应 |
|---|---|---|
| 借用检查器学习曲线陡、需"背下来" | Rust | GC 为默认;借用检查只出现在显式 `own` 块内 |
| 错误处理冗长、nil 遍地 | Go | `Result + ?`、无 null、sum types |
| 类型是事后补丁、不健全逃生舱(`any`) | Python/TS | 静态强类型、健全、无 `any` |
| 性能与 GIL、打包混乱 | Python | AOT 原生码、单二进制、cargo 式包管理 |
| 内存安全、UB、无包管理 | C/C++ | 默认内存安全;C ABI 一级互操作 |
| async 函数染色 | Rust/JS | 无 async/await 关键字,无色轻量任务 |
| 编译慢毁反馈循环 | Rust/Kotlin/Swift | 查询式增量编译;编译速度对特性有否决权 |
| node_modules/生态碎片 | JS/TS | 官方一体化工具链 + 严格 semver + lockfile |

### 1.3 AI 时代的直接证据

- 语言间生成质量差距大:MultiPL-E 上 Python 最高、Rust 历史最低(~40% pass@1),主因训练数据量与语言难度(arXiv 2410.03981)。
- 编译器反馈闭环让 agent 能自我迭代修复,HN 社区已有"Go 是 agent 最佳语言"的讨论。
- Armin Ronacher《A Language For Agents》(2026-02)实证结论:agent 偏好花括号(缩进被 tokenizer 破坏)、Result 而非异常(agent 倾向全 catch 后烂恢复)、可 grep 的包前缀符号、显式效果声明、一条命令 lint+build+test;讨厌宏、barrel 重导出、别名、flaky 测试。
- AI 生成代码的 CVE 在上升(CSA 2026 报告),编译期护栏价值放大。

### 1.4 硬性需求清单(本项目)

R1 原生跨平台(OS/桌面/服务器/移动侧) R2 Web(Wasm)一等公民 R3 嵌入式(裸机、硬实时可达成) R4 性能匹敌 C/Rust(热点路径追平;GC 档 ≤15% 为带退出条件的目标) R5 低内存、低能耗、充分发挥硬件(SIMD/多核/NUMA/io_uring) R6 可扩展、插件化(编译器/工具链/运行时三层) R7 AI 生成一次通过率高、人类阅读负担低 R8 快编译、快反馈 R9 完整独立(不依附宿主语言生态),C ABI 互操作

---

## 2. 总体路线选择(已决策)

### 路线 A(选定):单一语言 + 三档运行时 profile

同一门语言、同一套语法与类型系统,通过**运行时档位**适配场景:

- `full` 档:并发 GC + 轻量任务 + 完整 stdlib(服务器/桌面)
- `web` 档:编译到 WasmGC,GC 用宿主,任务映射到 JSPI/wasm threads
- `bare` 档:无 GC、无隐式分配、显式 arena/region,裸机可硬实时

内存模型:**GC 默认 + `own` 块所有权逃生舱**。热点代码用 `own (arena) { ... }` 切换到编译期借用检查的 Rust 子集,无生命周期标注(块内局部推断,通常成功;失败时给出机械降级建议,不引入生命周期标注,详见 §5.3)。

- 优点:AI 一次通过率高(无全局生命周期);热点与嵌入式仍可零成本;一门语言通吃全部目标。
- 代价:GC 与 own 的边界规则要设计清楚(§5.3);编译器要做两套内存检查。
- **性能口径(R4 落地)**:own/热点路径与 C 互有 5% 内(硬指标);GC 档基准 ≤15% 为**带退出条件的目标**——赌的是"分配消除"(逃逸分析 + 标量替换,§5.1),若 P3 阶段未达标则收紧 GC 档默认策略、引导热点走 own 块(§15 风险表同步)。
- **档位兼容方向**:bare ⊂ full 向上兼容——bare/own 代码可在任何档位使用;依赖 GC 的代码不可用于 bare(编译期由分配效果判定,§5.4)。

### 路线 B(否决):所有权默认(Rust 路线)

C 级性能与确定性最强,但调研显示 Rust 是 LLM pass@1 最低的主语言之一,且人类阅读时生命周期标注持续加税——与 R7 冲突。

### 路线 C(否决):VM/JIT 优先(JVM/Graal 路线)

移植成本低、峰值吞吐可期,但裸机与"匹敌 C"基本不可达,启动与内存与 R4/R5 冲突。JIT 仅作为脚本模式的辅助(§8.4)。

---

## 3. 设计原则(P1–P8)

- **P1 读优先**:代码被读的次数比被写的次数多两个数量级。所有语法歧义争议以"读者第一"裁决。
- **P2 护栏诚实化**:safe 子集内不存在不健全性(无 `any`、无 unchecked cast、无隐式指针)。**一切越界能力必须显式标记且 lint 可枚举**:FFI 与底层操作唯一入口是 `#[trusted]` 标记(§5.7),其数量在包级可审计。AI 的代码要么编译过,要么得到可行动的报错;不存在静默的不健全。
- **P3 一条命令**:`ctron build / test / fmt / doc / lint / bench / run / publish` 一体化(cargo 模式)。
- **P4 反馈毫秒级**:查询式增量编译从第一行代码开始;任何拖慢编译的特性不予接纳(编译速度否决权,Zig 原则)。
- **P5 效果显式**:分配、I/O、并发、可变性、错误——要么出现在签名里,要么是编译器可推断并在错误信息中展示的函数属性(分配效果见 §5.4;I/O 经能力对象见 §6.2),绝不停留在"读实现才知道"。
- **P6 可预测 > 表达力**:拒绝类型体操、宏魔法、多种等价写法。惯用法唯一化。
- **P7 零成本分层**:不用的能力不编译进产物;运行时按档位裁剪到 KB 级。
- **P8 可 grep**:符号一律"包路径前缀",禁止 wildcard 导出与 barrel 重导出。

---

## 4. 语法(v0 草案)

> 语法是草案,以语义决策为准;正式语法在 RFC 阶段冻结。

### 4.1 基本形态

- **花括号块**,显式分界(tokenizer/LLM 友好,证据见 §1.3)。
- 表达式导向:`let x = if ok { 1 } else { 2 }`。
- 命名:类型/枚举/构造子 `PascalCase`;函数/变量 `snake_case`;常量 `SCREAMING_CASE`;包名全小写单词。
- `let` 不可变绑定,`var` 可变绑定;`fn` 定义函数。**参数可变性**:`fn push(xs: var List<T>, v: T)`——默认参数为只读视图;`var` 前缀 = 函数内可变(可变类实例的独占访问规则见 §7.2)。
- 统一调用语法(UFCS):`xs.map(f)` 等价 `iter.map(xs, f)`——方法糖可作用于自由函数,链式可读且不膨胀 OOP。
- 字符串插值:`"Hello, {name}!"`(UTF-8;`.len` 为字节长度,`.char_len` 为字符数;**切片按字节索引且必须落在 UTF-8 字符边界,越界即 panic**;字素簇迭代提供只读 API)。
- 显式导入,具名、可追溯:`use std.net.{TcpListener, Request}`;禁止 `use mod.*` 与重导出。
- 注释即文档,文档示例即测试(doc-test,编译并运行)。

### 4.2 示例:常规业务代码(full 档)

```c
use std.http.{Server, Request, Response}
use std.time.Clock

fn handler(req: &Request, clock: &Clock) -> Result<Response, HttpError> {
    let name = req.query.get("name") or "world"     // Option 的显式默认,无 null
    log.info("hit /hello at {clock.now()}")          // 显式依赖注入的时钟,可 mock
    return Response.text("Hello, {name}!")
}

fn main() -> Result<Void, Error> {
    let server = Server.bind("127.0.0.1:8080")?
    server.run(handler)?
    return Ok(void)
}
```

读这段代码不需要看实现即可知道:会失败(`Result`)、错误类型是什么、依赖什么能力(`Clock`)、可能产生 I/O(经由能力对象)。

### 4.3 示例:模式匹配与 sum 类型

```c
enum Shape {
    Circle { radius: F64 }
    Rect   { w: F64, h: F64 }
}

fn area(s: &Shape) -> F64 {
    return match s {
        Circle { radius } => Math.PI * radius * radius
        Rect { w, h }     => w * h
    }   // 穷尽性编译期检查,新增变体 → 所有 match 报错
}
```

### 4.4 示例:热点路径 `own` 块(性能档)

```c
fn parse_all(input: &[U8]) -> Result<List<Event>, ParseError> {
    own (arena) {                        // 切换到所有权模式:无 GC、无写屏障
        var events = arena.list(Event)   // 显式 arena 分配
        var pos: USize = 0
        while pos < input.len {
            let (ev, next) = parse_one(arena, input[pos..])?   // 借用 arena,块内推断
            events.push(ev)
            pos = next
        }
        return Ok(events.into_gc())      // 只有显式 into_gc 的数据才进入 GC 堆(语义见 §5.3)
    }   // arena 整体释放,一个 free
}
```

### 4.5 错误包装(Rust `Box<dyn Error>` 之痛的回应)

- 错误类型需实现内建 `Error` trait:`message` + `cause: Error?`(链)+ `context(text)` 附加器。
- `?` 传播时**自动附加调用点位置**(文件:行号,release 可关),错误链自动累积——排查 AI 生成代码的失败路径时,链条完整且零样板。

---

## 5. 内存管理(核心章节)

### 5.1 默认层:低延迟并发 GC(full/web 档)

- **分代、并发、三色**,精确栈;目标 P99 停顿 < 0.5ms、与堆大小无关(Generational ZGC 已证明可达)。
- 年轻代分配走 **mimalloc 式线程本地 size-class 分配器**——小对象分配 ~10ns,无锁。
- **分配削减优先于回收优先**:值类型 + 就地构造 + 逃逸分析使"分配数"本身趋近零,GC 只是兜底(证据:大量 `Arc<Rc>` 的 Rust 反而不如批量 GC 快——把小对象GC做好比强制手动管理更优)。
- GC 可插拔(运行时接口标准化):默认并发分代;可换 引用计数(移动端省内存)/ 无 GC(bare 档)。v1 落地顺序:先并行复制分代 GC(STW 年轻代 + 并发老年代),再演进并发化——避免一上来就造 ZGC 级复杂度。
- 卡片:GC 档的"接近 C"路径 = 逃逸分析 + 标量替换 + 分配消除,让热路径对象根本不上堆;读/写屏障的既有开销(业界 5–15%)与 §2 的 ≤15% 目标口径一致,这也是 own 块存在的理由。

### 5.2 值/引用二分(语法层可见,读者一眼分清)

- `struct` = **值类型**(栈/内联,赋值即拷贝,编译器消除冗余拷贝)。
- `ref class`(简称 `class`)= **GC 堆引用类型**。**字段默认不可变(`let` 字段);可变字段必须显式 `var` 字段**——这一条是 §7.2 数据竞争消除的基石。方法默认拿 `&self` 只读视图,`var self` 才可变。
- **struct 含引用字段的别名语义(明确规则)**:合法;拷贝为**浅拷贝**,内部类引用被共享(与 Swift 一致)。`ctron lint` 对"含 `var` 类字段字段的 struct"默认给 W 级提示,引导改用不可变字段或枚举。文档必须明示此语义,不给"深拷贝错觉"。
- 装箱显式:`Box<T>`。数组/切片 `T[]`、`List<T>` 惰性,无隐藏堆分配。

### 5.3 `own` 块:作用域所有权子集(性能逃生舱)

- 语法 `own (alloc) { ... }`,`alloc` 是 `Arena | Region | Pool | Static`(块自带全新 arena;嵌套 own 块允许,arena 链式父子)。
- 块内规则 = Rust 的一个**可判定子集**:move 默认、borrow 检查、无生命周期标注。局部推断**通常成功**;失败时错误信息给机械降级建议(拷贝值 / 改用 GC 值 / 提参数),不与用户缠斗,不引入生命周期标注。
- 块外规则:出块仅 `Copy` 或显式 `into_gc()`;GC 值禁止在块内被可变借用(防止跨 GC/own 边界的隐藏别名)。
- **`into_gc()` 语义(v0.2 明确)**:
  - **(a) 语义转换(默认,v1 实现)**:深拷贝进 GC 堆。简单、可预测;成本 O(数据量),调用处显式可见。
  - **(b) arena 晋升(优化路径,v2)**:arena 存活对象带类型描述符,GC 一遍扫描后 copying 进堆,免去拷贝。需要运行时配合,列为运行时优化项。
  - 设计含义:own 块最适合"进大出小"的负载(解析过滤聚合、批量计算、缓冲处理);"构造大结果返回"的场景在 v1 付一次出口拷贝,若 profile 显示是热点,用 (b) 或改在 GC 堆就地构造。
- 面向 AI 的护栏:own 块借用报错的修复建议是机械的(拷贝/移动/改 GC),不会像全局生命周期那样把架构掀翻。

### 5.4 分配效果追踪(own/bare 检查的机制基础,v0.2 新增)

- 每个函数有编译器推断的**分配属性**:`alloc`(含 GC 分配)或 `no_alloc`。直接调用链可精确推断;**经 `dyn Trait` 不可精确判定时保守视为 `alloc`**。
- trait 方法可显式标注 `#[no_alloc]` 作为**契约**:所有实现必须遵守,违者编译错误。这使"无分配接口"(如 ISR 回调 trait)可被库作者发布。
- 检查点:`own` 块内、`#[no_alloc]` 函数体内、bare 档全局——调用 `alloc` 函数 = 编译错误,错误信息附修复建议(把堆数据改为显式 arena 参数传递)。
- 该属性展示在文档与 LSP hover 中(落实 P5),不增加签名书写负担(默认推断)。

### 5.5 所有权与资源(RAII 硬规则)

- 析构确定性:文件/锁/socket 用 RAII(编译期 drop,无需 GC 参与);GC 只管内存,不管资源。
- **硬规则(v0.2)**:`class` 不得直接持有需确定性释放的资源;资源只能由值类型/句柄包装(`struct` + 编译期 drop)持有,或经能力对象的生命周期管理。违者 lint E 级。这条规则让"资源泄漏"在 GC 语言里成为编译期可见的类型错误,而非运行期不确定行为。

### 5.6 `bare` 档:完全显式

- 无 GC、无运行时分配器(可选静态池);所有分配经显式 arena 参数(Zig 模式)。
- `#[no_alloc]` 函数约束:隐式分配 = 编译错误(机制见 §5.4);中断服务例程默认 `#[no_alloc] #[no_spawn]`。
- 硬实时达成路径:零隐式分配 + 无 GC 停顿 + 可确定性构建。

### 5.7 FFI 与 `#[trusted]`(v0.2 新增)

- `extern "c"` 与指针操作是本语言**唯一的不健全入口**,必须写在 `#[trusted]` 标记的函数/块内;`ctron lint --trusted` 枚举一个包的全部信任边界(数字 + 位置),包发布时随元数据上报。
- **FFI 内存所有权约定**(三类,bindgen 按此生成):
  - `C-owned`:C 分配 C 释放,Ctron 只借用在调用期(裸指针参数);
  - `Ctron-owned`:跨边界移交所有权时必须经包装类型(如 `CBox<T>`),明确 drop 责任方;
  - `borrowed`:临时借用,类型系统标注生命周期为调用期(FFI 包装层内,不外泄)。
- 禁止把 arena 内存直接交给 C 长期持有(arena 释放后即悬垂);需要时深拷贝出边界。

---

## 6. 类型系统

### 6.1 决策表

| 决策点 | 选择 | 理由 |
|---|---|---|
| 名义 vs 结构 | 名义类型 + 名义 trait(显式 impl) | 报错定位、可 grep、文档自明;TS 结构型的教训 |
| null | 无 null;`Option<T>`(糖:`T?`),必须解包 | 十亿美元错误,健全 null 安全是社区最高共识愿望 |
| 错误 | `Result<T, E>` 在返回类型里 + `?` 传播(自动上下文,§4.5);panic 仅表 bug,只在任务边界可捕获 | Joe Duffy《Error Model》+ Ronacher 的 agent 证据 |
| 泛型 | 单态化默认,`dyn Trait` 显式动态分发 | 零成本;动态分发是读者的显式信号 |
| HKT | 不做 | 类型体操与 R7 冲突;GAT 级别也不进 v1 |
| 推断 | 块内全推断;**签名必须标注** | 签名即契约(AI 与人都靠它局部阅读) |
| 继承 | 不做;组合 + trait | 现代共识 |
| 运算符重载 | 不做(数值/定长 SIMD 白名单除外) | 可预测性;硬件语义需要 `*` 于向量 |
| 渐进类型 | 不做 | sound gradual typing 有数量级减速实证(POPL16),且 `any` 是 AI 代码劣化的入口 |
| 元编程 | `comptime`(Zig 式统一),见 §6.3 | 不另造宏语言;proc-macro 是 Rust 编译慢主因之一 |

### 6.2 效果系统 = 能力对象(capabilities),而非新类型系统

I/O 类效果建模为**必须持有的能力值**:`fn read(path: Path, fs: &Fs) -> Result<Bytes>`。

- 可读:依赖出现在参数里,读者一眼看到(比 effect 关键字更少仪式)。
- 可测:测试注入 `FakeFs`,无需 mock 框架。
- 可审计:manifest 声明程序可用能力(`Ctron.toml` 权限段);main 从运行时初始化处按 manifest 获得能力,越权使用 = 编译期检查(所用能力超出声明集)。
- 内建注解只保留三个:`#[no_alloc]`、`#[no_spawn]`、`#[pure]`(可被编译器用于优化与并行证明)。
- **已知代价(v0.2 注记)**:能力经长调用链显式传递会产生 Go `context.Context` 式样板。缓解:库 API 把能力参数集中在入口构造、任务局部隐式传递**仅限**同一任务内且在类型上可见(编译器检查,不跨任务)——此机制在 RFC 阶段细化,若做不干净就接受样板(诚实优于隐式)。

### 6.3 comptime:有边界的编译期执行

- 泛型即 comptime 类型参数:`fn Matrix(comptime N: USize) -> type`。
- comptime 函数在 CVM(§8.4)上执行,**纯函数 + 总时间预算**,超时/副作用 = 编译错误(杜绝 proc-macro 式无限膨胀与不可缓存)。
- **保持 parametricity**:comptime 不得反射泛型参数的运行时类型(Zig 的教训);类型反射仅限显式 `@derive(Json, Eq)` 声明,由注册的 derive 插件(§10)展开为普通代码。

### 6.4 数值

- `I8..I64 / U8..U64 / ISize / USize / F32 / F64`;默认**检查算术**(溢出 = panic,可 release 关闭);`x +% y` 显式回绕;`Simd[F32, 8]` 定长向量一等公民。

### 6.5 模块系统与 trait 一致性(v0.2 新增)

- **结构**:一个文件 = 一个模块;目录 = 命名空间模块;`use` 路径 = `包名.模块.符号`,与文件树一一对应(可 grep 的物理保证)。
- **可见性**:默认模块私有;`pub` 对外导出;`pub(pkg)` 包内可见。三级,向前兼容。
- **trait 孤儿规则(coherence)**:impl 合法当且仅当 trait 或被实现类型至少一方定义于当前包;无例外、无泛型豁免。理由:杜绝 Rust 生态的 impl 冲突与下游"补 impl"怨案;配合 §7.2 的 Send 自动推导,库边界清晰。
- 循环依赖在包与模块两级均禁止(与 §8.2 编译模型一致)。

---

## 7. 并发(无色)

### 7.1 模型

- **轻量任务 + work-stealing 调度**(goroutine 式),无 async/await 关键字:所有代码一种颜色;可挂起点由编译器标记,底层用**可增长连续栈(拷贝式,Go 1.3+ 现行方案;非已被否定的分段栈)**——挂起才付切换成本。
- **结构化并发作用域**:`scope { |s| s.spawn(...) }` 块退出即 join;子任务失败 → 取消兄弟 → 错误沿作用域树传播(取消是一等公民)。
- **通道** CSP 原语;有界默认(背压显式);**向已取消作用域的 send/recv 返回 `Err(ScopeCancelled)`**(不 panic,不静默)。
- **数据并行单独建模**(Rayon 证据:与 I/O 并发不可混用):`parallel.map / parallel.reduce`,迭代器自动 SIMD 化 + 分块窃取。
- **栈溢出策略**:full 档任务栈上限默认 1MB(可配),触顶 = 任务边界 panic;bare 档静态栈 + guard page,溢出 = trap(部署配置决定复位/记录)。

### 7.2 跨任务共享与数据竞争消除(v0.2 新增,核心机制)

GC 档没有借用检查,引用可自由复制——因此"可变共享"必须由**类型规则 + 强制检查点**拦下,而非运行时。规则如下:

- **Send 的定义**(编译器自动推导,不可手工实现):
  - 纯值类型(Copy)→ Send;
  - `class` 所有字段均为 `let` 且字段类型递归 Send(深度不可变)→ Send;
  - 含任一 `var` 字段的 class → **非 Send**;
  - `Mutex<T>` / `Atomic<T>` 包装 → Send(T 本身任意)。
- **强制检查点(三处,编译期硬检查)**:
  1. `spawn` 闭包捕获的每个值必须 Send;
  2. channel 的收发值类型必须 Send;
  3. 非 Send 类型不可作为 `static`/全局存储。
- **由此得到的保证**(无 `#[trusted]` 介入时):任何被两个任务同时触达的数据,要么深度不可变,要么在锁内——**可变实例的所有引用天然被限制在单一任务内**(非 Send 类型无法越界),任务内自由可变无竞争。数据竞争在编译期消失,且不依赖生命周期标注。
- 与 Swift Sendable 的差别:Swift 因 ObjC 互操作只能做建议性检查;Ctron 无历史包袱,**硬检查**。与 Rust 的差别:无生命周期参与、错误信息机械可修("字段 b 为 var → 加 Mutex 或改 let")。
- 兜底:`#[trusted]` FFI 可破坏此保证,审计口径同 §5.7。
- **web 档注记**:单线程事件循环下 JS 回调不构成并行竞争,Send 检查按编译期可判定的近似执行(共享一切、禁止阻塞);wasm threads 启用时恢复全量检查。

### 7.3 全局状态(v0.2 新增)

- `static let`(不可变全局)合法,初始化为编译期常量或首次访问的 `pure` 惰性求值。
- **`static var` 不存在**。可变全局只有一条路:显式 `Global<T>` 注册(内部即 Mutex),带能力审计——全局可变状态必须"看得见"。

### 7.4 硬件利用(R5)

- io_uring / kqueue / IOCP 统一异步 I/O 层(运行时内建,API 无色)。
- NUMA 感知分配器与任务亲和(运行时选项);`Simd` 类型 + 自动向量化;comptime 展开保证热点无循环开销。
- GPU:`kernel` 块编译到 SPIR-V/PTX(v2 路线,MLIR 插件,§10),不进 v1 核心。

---

## 8. 编译器与工具链架构

### 8.1 分层

```
源码 → 手写递归下降解析(快、报错好)
     → HIR → 类型检查/效果检查/Send检查(查询式,Salsa 式增量)
     → CIR(SSA,携带效果/分配信息,单 IR 多用途)
     → 后端(全部插件化,§10):
        ├ Cranelift  → dev 构建(比 LLVM 快 ~40%,产物慢 ~14%)
        ├ LLVM       → release 构建(-O2/-O3 + LTO + PGO)
        ├ WasmGC     → web 档;Wasm MVP → 嵌入式 wasm 运行时
        └ CVM 解释器 → comptime 执行 / ctron run 脚本模式 / 调试
```

### 8.2 编译速度(P4 否决权的落地)

- 查询式编译 + 持久化增量缓存(rust-analyzer 的 durable incrementality 经验)。
- 语言级保证:**禁止循环依赖**(包与模块两级,§6.5)、无头文件、单遍可解析、泛型实例化成本受控(单态化预算超限 → 提示 dyn)。
- 内容寻址的全局构建缓存,CI 直接命中。
- 预算:中型项目(10 万行)增量 check < 200ms;全量 debug 构建 < 5s。

### 8.3 一条命令工具链(P3)

`ctron build/test/fmt/doc/lint/bench/run/publish/add/target`;严格 semver + lockfile + workspace;`ctron fmt` 输出**唯一规范格式**(AI 产物自动归一);`ctron check` 输出机器可读 JSON 诊断(每条带稳定错误码如 `E1024` + fix 建议),这是给 agent 的第一接口。

### 8.4 CVM(共用虚拟机)

一个小型栈式 IR 解释器,同时服务 comptime 执行、脚本模式(`ctron run xxx.ct` 零等待启动)、教学调试;不承诺性能,不进生产路径。

### 8.5 产物与体积口径(v0.2 修正)

- 默认静态单二进制;交叉编译内建(`ctron build --target riscv32imac-none`)无需外部工具链(lld 内嵌,含 minilibc 选项)。
- **体积指标分档**:bare + core 运行时 < 100KB(硬指标);full 完整运行时(并发 GC + 调度器)< 1MB(目标)。v0.1 的"300KB 全运行时"与并发 GC 功能清单不符,已修正。

### 8.6 引用实现与自举(v0.3 修订:早自举、流式移植)

- 引用编译器 ctron0 以 **Rust** 实现:LLVM/Cranelift 绑定现成、Salsa 增量框架可复用、生态成熟降低 P1/P2 风险。
- **自举政策(用户已确认)**:早自举、流式移植——P1 完成后立即开始把编译器逐模块移植到 Ctron,每移植一个模块即差分验收并**删除对应 Rust 模块**,禁止长期双实现。
- **自举对象切分**:自举**前端主干**(词法/解析/类型检查/CVM/工具链);**codegen 后端是经 C ABI 调用的库**(Cranelift/LLVM,永不重写),**GC/调度运行时**先是链接库、移植为远期可选——与 rustc/Go/Zig 先例一致。
- 验收机制:61 文件测试语料 = 差分金丝雀(两代编译器对同一语料的 AST/诊断/产物必须逐字节一致);确定性构建(§10.3 spec)使"固定点验证"(ctronN 编译自身,产物逐位一致)成为自举闭环的判定。

---

## 9. 跨平台矩阵(R1/R2/R3)

| 档位 | 目标 | 内存 | 并发 | stdlib 层 |
|---|---|---|---|---|
| `full` | Linux/macOS/Windows/移动 NDK | 并发分代 GC(可插拔) | 任务+通道+并行 | `core < alloc < std` |
| `web` | WasmGC(浏览器/边缘/插件沙箱) | 宿主 GC | JSPI/wasm threads | `core < alloc < stdweb` |
| `bare` | Cortex-M/RISC-V/裸机 wasm | 无(arena/静态池) | 可选协作调度器库 | `core` |

- 分层同 Rust `std/no_std` 但按**档位**而非特性开关组织;同一 crate 的代码可声明"至少需要哪层"。
- **兼容方向(v0.2 明确)**:bare 代码可用于 full/web(向上兼容,§2);依赖 GC 分配的代码编译进 bare 即报错(§5.4 机械判定)。own 块代码任何档位可用。
- JS 互操作:类型化桥(无 `any` 泄漏;JS 异常在边界转为 `Result`,JS 回调按 §7.2 web 档 Send 近似规则检查);C ABI 一级互操作(`extern "c"` + `ctron bindgen` 从 C 头生成绑定,所有权约定见 §5.7)。

---

## 10. 插件化与扩展性(R6)——"编译器本身即插件宿主"

三层插件协议,**全部稳定 API**:

1. **编译器插件**(在 CVM/wasm 沙箱内运行,保证确定性与可缓存):
   - `derive` 插件:实现 `@derive(Json, Eq)` 类代码生成(受 comptime 纯函数约束);
   - `lint`/诊断插件:拿到类型化 HIR,产出带错误码的警告;
   - **codegen 后端插件**:Cranelift/LLVM/Wasm 本身就是首批插件——第三方可加自定义后端(如 GPU/DSL),这是"可扩展"的最大杠杆。
2. **工具链插件**:`ctron <子命令>` 外部可执行(git 模式);LSP server 内建,插件可扩展语义面板。
3. **运行时插件**:稳定 C ABI 动态库 + IDL 接口描述,支持热重载(桌面/服务器档)。

配套生态基建 day-1:tree-sitter 语法 + LSP + 语言服务器协议;后续 v2:面向 agent 的 MCP 服务(check/fix/docs/语义 diff)。

---

## 11. AI 原生设计(R7,差异化核心)

1. **语法面向 tokenizer**:花括号、无缩进语义、无上下文相关语法;`ctron fmt` 保证 AI 产物归一到唯一格式。
2. **错误码体系**:每条诊断有稳定码 + 复修建议(示例先行),供 agent 循环消费;`ctron check --format=json` 一条命令完成 parse+type+lint。
3. **契约可读**:签名必标注 + 能力参数 + `#[pure]`/`#[no_alloc]` 注解 → 人审查 AI 代码时读签名即可圈定行为面。
4. **语义 diff**:`ctron diff` 输出结构化(签名/效果/行为三类)变更报告,服务"人审 AI PR"场景。
5. **doc-test 即回归**:文档示例全部编译运行,AI 改动立即可验证。
6. **反 AI 穴位清单**(明确禁止的坑):无 barrel/别名重导出、无宏、无 flaky 默认(时间/随机必须经能力注入)、无"类型错还能跑"(TS 误导 agent 的实证教训)。
7. **确定性模式**:`ctron test --deterministic` 冻结调度序与哈希种子,失败可复现。

---

## 12. 标准库草图

- `core`(全档位):数值、切片、`Option/Result`、惰性迭代器、模式匹配、`comptime`、`fmt`、UTF-8 字符串、`Error` trait(含 cause 链与 context)。
- `alloc`:`List/Map/Set/Box/String`(显式依赖分配器参数——bare 档也能用,只要给 arena)。
- `std`:`fs/net/http/json/log/crypto/time/test`——全部 I/O 经能力对象;测试框架内建(属性测试 + 快照);`Mutex/Atomic/Global/channel` 并发原语(§7)。
- `stdweb`:DOM/Canvas/Fetch 桥(类型化)。
- 治理:editions 机制演进不破坏;规范 + 一致性测试集开源。

---

## 13. 明确拒绝清单(与理由)

异常(控制流不可读)、null、`any`/渐进类型、HKT、文本宏/AST 手术、async/await 染色、继承、任意运算符重载、循环依赖、wildcard 导入、隐式数值转换、资源依赖 GC 回收、`static var` 可变全局(§7.3)、隐式不健全操作(仅 `#[trusted]`,§5.7)。

---

## 14. 实施路线图(v0.3 重排:自举阶梯,早自举流式移植已确认)

| 阶段 | 内容 | 出口标准 | 状态 |
|---|---|---|---|
| P0 | 规范冻结 + 一致性测试集 | 语言符合性 100% | ✅ 2026-09-04(v0.5 + 61 测试) |
| P1 | **ctron0**(Rust):词法→解析→类型检查→CVM→测试 harness | 61 文件按 README 规则全绿 | P1-A 词法器计划已立项 |
| P1.5 | **自举基建**:CIR 序列化格式冻结;差分 harness(金丝雀比对);editions v1 冻结 | 两代编译器语料产出逐字节可比对 | |
| P2 | Cranelift 后端(**C-ABI 库形态**)+ full 运行时;**流式移植开始**:词法器→解析器逐个移植+差分验收+删 Rust 对应模块 | benchmarks-game 子集达 C 的 2x 内;词法/解析自举 | |
| P3 | LLVM 后端 + own 块 + 分配消除;类型检查移植 | own 档 ±5%;GC 档 ≤15% 目标评审 | |
| P3.5 | **CVM 移植**(Ctron 写的解释器,原生编译) | `ctron run`/comptime 自举 | |
| P4 | WasmGC 档 + JS 桥 + stdweb | `10_web_dom.ct` 等锚定转绿 | |
| P5 | bare 档:交叉编译 + no_alloc 体系 + 板级 demo | blink + 硬实时环路 | |
| P6 | **固定点**:ctronN 编译自身产物逐位一致 → 删除 Rust 宿主代码;插件 API 稳定化 + 包仓库 | 自举闭环;第三方插件可发布 | |

### 14.1 自举阶梯(流式规则)

```
ctron0(Rust 全量,P1)
  └─ 移植词法器 → 差分验收(参照物 = Rust 词法器 + 61 文件金丝雀)→ 删 Rust 词法器
  └─ 移植解析器 → 差分验收 → 删
  └─ 移植类型检查(P3 期,Cranelift 已可用)→ 差分验收 → 删
  └─ 移植 CVM(P3.5)→ ctron 原生编译自己
固定点:ctronN 编译 ctronN → 产物逐位一致(确定性构建使然)→ 自举闭环
```

- **规则一**:任一时刻同一模块只有一份权威实现(移植完成即删 Rust 版),差分测试只存在于移植窗口内。
- **规则二**:后端永远经 FFI 库调用(§9.6 `extern "c"` + `#[trusted]`,边界收敛 3~5 个函数,`ctron lint --trusted` 审计);CIR 序列化格式是前端↔后端的稳定契约(P1.5 冻结)。
- **规则三**:移植优先用 agent 执行——参照物 + 金丝雀差分就是为 agent 设计的判分回路;人负责裁决金丝雀不一致时的语义疑点。
- CVM 期自托管编译器较慢(分钟级)可接受;Cranelift 就绪后消失。

## 15. 主要风险与对策

| 风险 | 对策 |
|---|---|
| GC 档难达"≤15%"(读/写屏障既有开销 5–15%,目标本就顶满) | **口径已改为带退出条件的目标**:分配消除(逃逸分析+标量替换)为主、GC 为兜底;P3 未达标则收紧 GC 档默认策略并引导热点走 own;硬指标只压在 own/热点路径(±5%) |
| own 块出口 `into_gc()` 拷贝吃掉收益 | 语义已明确(§5.3):v1 深拷贝、v2 arena 晋升;引导 own 用于"进大出小"负载 |
| Send 硬检查在互操作边界(JS/DOM 回调、FFI)漏检或过严 | web 档单线程近似规则(§7.2);FFI 归入 `#[trusted]` 审计(§5.7);规则保持机械可判定,拒绝启发式 |
| own 子集与 GC 边界复杂 | own 规则设计为"可判定、可降级":推断失败建议改 GC 值,不与用户缠斗 |
| 双后端维护成本 | 后端从第一天就是插件接口;Cranelift/Wasm 各自独立测试矩阵 |
| 范围失控(系统级语言 + 全平台) | 严格按 P0→P6 阶段门禁;每阶段出口量化 |
| 冷启动生态 | 证据表明 agent 可移植既有库;C ABI + bindgen 先行,让"用存量 C 库"成为第一天能力 |
| 双实现漂移(自举窗口期) | 流式规则:移植完成即删 Rust 版,差分测试只存在于窗口内;61 文件语料为金丝雀 |
| 语言冻结前移植导致返工 | P1.5 冻结 editions v1;移植目标限定冻结版;演进走 editions 机制 |
| CVM 期自托管编译器慢(分钟级) | 可接受(bootstrap 期专用);Cranelift(P2)就绪后消失;差分验收不依赖速度 |

## 16. 术语表(本设计引入的概念)

`own 块` 作用域所有权子集 / `档位(profile)` full|web|bare / `能力对象` 显式注入的 I/O 权限值 / `CIR` SSA 中间表示 / `CVM` comptime 与脚本用共用解释器 / `derive 插件` 沙箱内代码生成器 / `Send` 可跨任务迁移的类型属性(深度不可变或锁包装,§7.2) / `#[trusted]` 唯一的不健全标记,lint 可枚举审计(§5.7) / `into_gc()` own 块数据进入 GC 堆的显式出口(转换/晋升两路径,§5.3) / `分配属性` 函数的 alloc/no_alloc 推断属性(§5.4)。

## 17. 修订记录

- **v0.1 → v0.2(2026-09-04,评审修订)**:
  1. 新增 §7.2/§7.3:Send 定义、三处强制检查点、可变全局规则——兑现"数据竞争编译期消失"。
  2. 新增 §5.4:分配效果追踪机制;§5.3:into_gc 两条路径;§5.5:RAII 资源硬规则;§5.7:FFI 所有权约定与 `#[trusted]`。
  3. 新增 §6.5:模块系统、可见性、trait 孤儿规则;§8.6:引用实现宿主语言(Rust)与自举政策。
  4. 修正:分段栈→可增长连续栈;"推断必成功"→"通常成功+机械降级";运行时体积 300KB→分档(100KB/1MB);GC ≤15%→带退出条件的目标,硬指标移至 own 档(±5%)。
  5. 补遗:错误包装(§4.5)、参数可变性(§4.1)、struct 引用字段别名规则(§5.2)、channel 取消语义与栈溢出(§7.1)、字符串切片规则(§4.1)、档位兼容方向(§2/§9)、能力传递样板注记(§6.2)。
