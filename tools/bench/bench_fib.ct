fn fib(n: I32) -> I32 {
    if n < 2 { return n }
    return fib(n - 1) + fib(n - 2)
}
test "bench_fib" {
    var i = 0
    var acc = 0
    while i < 80000 {
        acc += i % 7
        i += 1
    }
    assert_eq(fib(20), 6765)
    assert_eq(acc, 239994)
}
